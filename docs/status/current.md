# RadishAxiom 当前状态

更新日期：2026-08-31

## 当前阶段

项目处于首域语义、Axiom IR v0.1、Axiom Evidence v0.1、版本身份分层、四题版本化基准语料、Agent 对比实验预注册、`raxc` 生产实现语言、首个验证后端、首个目标执行路径、首版编译管线和独立 checker 隔离边界都已经形成的设计到受控实现阶段。checker request / bundle / result 的结构契约、工具链 / adapter 元数据身份清单、pipeline artifact 契约、实现就绪场景矩阵、四题完整 checker 离线 bundle，以及 cvc5 / Node / checker 的 options / limits 与 certificate 空能力矩阵已经物化。Go `go1.26.7` macOS arm64 host/source 两个精确 payload 已完成局部供应链验收；独立 Git 仓库 `RadishAxiomChecker` 已形成严格 request / manifest、只读 bundle、`checker.source`、Axiom IR v0.1 严格结构与类型良构、Axiom Evidence v0.1 严格结构与身份、独立 obligation definition / ID 集合重建、expectation / 五态 / execution / tool / attempt / trust / support 闭合检查、counterexample 有限 world 与 `WF` 检查、concrete input artifact 重建和 assume / `Pre` 求值、锁定有限 IR DAG 的确定性执行和 proof-failure 目标重放、host / golden output artifact 重建与三方独立比较、proof support artifact 真值审计与空 kernel / certificate 能力边界、production conclusion 的确定性重算与精确 refs 比对、十类 check / remaining trust / missing artifact / 四态独立结果的内存聚合、Independent Check Contract v0.1 canonical companion encoder / 严格 parser / 自身份复算 / 外层 invocation failure codec、贯穿前置身份、十类检查和编码前门禁的完整 invocation 累计资源账本，以及只接受 `check --bundle-root=<canonical-realpath>` 的唯一产品 CLI。
CLI 已在 bundle I/O 前绑定 linker 注入的 source / 精确版本、进程实际 `go1.26.7` toolchain、自身 executable SHA-256 与 runtime TCB，并严格区分 canonical stdout result、无结果诊断和外层进程失败。首个 macOS arm64 `0.1-dev` payload 已由精确官方 Go 归档完成双次隔离、原始字节一致构建，并由不调用编译器的独立 acceptance 检查 Mach-O / Go build info / 自身份和 normal / rejected / incomplete 三条真实 CLI 路径，决定为 `accepted-for-controlled-runtime-registration`。20 个 `failed` counterexample 条目已按真实语义边界全部动态收口；213 个 producer `proved` claim 也已区分为 65 个 attestation-only 与 148 个缺少可检查材料的 kernel claim，独立证明数保持为 0。24 个前置完整场景的 production conclusion 已得到 7 个 `satisfied`、4 个 `input_rejected`、8 个 `violated`、4 个 `inconclusive` 与 1 个 `implementation_inconsistent` 的独立匹配；25 个实际进入结果层且具有 expected companion 的场景由真实 findings 形成 22 个 `accepted-with-trust`、2 个 `incomplete` 与 1 个 `rejected`，并全部完成 actual result 的 canonical round-trip；27 份指定态 companion 也全部由 checker parser 独立接受。
Checker Runtime Payload Registration v0.1 已冻结 source / target / artifact / provenance / acceptance / retention / fetch / re-verification 边界，但 active runtime 仍为 0：最早的候选因 source 过期且字节未保留而不可登记；旧 `checker.source = sha256:e2c4ae...9044` 的有限期候选只有内层 payload，不能重绑到当前 source 或直接发布。自包含 distribution package、独立 distribution acceptance 与外层严格 verifier 已由 merge commit `f960603...13d5e` 同步进入 Checker `master` / `dev`；当前 `checker.source = sha256:401158...e3999` 已通过 `Checker Payload Candidate` run `33301288846` attempt 1 的精确 `go1.26.7` 双次隔离构建、payload / distribution acceptance、外层 direct-file 上传与 Ubuntu 按 artifact ID 独立回读。首个 durable provider 已按分别授权完成 repository immutable setting、精确 draft、唯一 asset 上传 / 回读和单次发布；Release ID `379226889` / asset ID `536372439` 为 `immutable = true`，GitHub Release / asset attestation、发布后 authenticated 与公开双路径回读、外层 / 内层严格 verifier 均通过。主仓已在独立状态转换授权下把该精确 payload 登记为 `registered-inactive`，绑定持久字节与重新验证路径但不允许 launcher 选择。ADR 0011 与 canonical launcher policy 现已冻结 active-only 的 `darwin / arm64 / v8.0 / macho-64-arm64` 精确选择、content-addressed 原子安装、三条 qualification companion、每次 spawn 前后身份复核和外层失败不形成四态的边界；依赖无关的 Python 一致性核心已在合成 USTAR、文件系统、qualification companion 和进程观察上将这些决策变成离线回归，但它不是生产 installer / launcher，完整策略状态仍为 `specified-not-implemented`。ADR 0012 进一步把产品侧实现冻结为主仓 Rust 生产图中的内部组件，固定无网络 installation core、产品注入的 `checker-runtime-store-v0.1`、单一 Rust result consumer 和 Checker Go parser / Python runtime 不复用边界。首个原生 Rust 纵向切片现已物化：根 `rust-toolchain.toml` 精确固定 `1.97.1` minimal + `rustfmt` / `clippy`，virtual workspace 与 Cargo 生成 lockfile 只含 `publish = false` 的 Rust 2024 library `radishaxiom-checker-runtime`，production / dev dependency 均为 0。crate 以 safe Rust 自有 SHA-256 和闭合 ASCII canonical JSON 子集严格读取完整 policy / current registration 对象形状，独立重算既有 Python fixture 的域摘要，形成 typed native target、registration identity 与 qualification / product active-only selection；13 项 Rust 测试和 19 项 Python oracle 覆盖 duplicate / unknown / noncanonical JSON、摘要漂移、unknown target、Rosetta / architecture / variant mismatch、inactive 产品拒绝、重复 target 与 ambiguous selection。当前任务进程的 `RUSTUP_TOOLCHAIN=1.96.0` 覆盖会先于仓库 pin 生效，因此正式 Cargo 门禁均显式使用已验收的 `+1.97.1-aarch64-apple-darwin`，没有把 `1.96.0` 结果计入证据。下一连续实现需从 strict outer / inner archive、installation receipt 与 store transaction 中再收敛一个独立切片；真实 checker 下载 / 安装、payload 执行与 `registered-inactive -> active` 仍须分别验证和授权。当前结果不检查 counterexample minimality，不代表 kernel rule 或 certificate 已验真、backend attestation 已升级为独立 proof、durable payload 已安装 / 激活、完整 launcher 已实现或六平台已经形成运行证据。

## 已确定

- 项目定位：面向 AI Agent 的验证优先语言与可信语义层。
- 命名：`.rax`、`raxc`、Axiom IR、Axiom Evidence。
- 核心原则：约束显式、信任可见、验证状态分层、证据可复核、小可信内核。
- 独立 checker 远程治理基线：公开仓库 `laugh0608/RadishAxiomChecker`；`master` 是默认稳定主线，`dev` 是常态集成分支，首次两个 refs 均精确指向治理提交 `28ad1dbe8c61b9a34985630b4f26d9a885711b3e`；merge commit / rebase merge 开启、squash merge 关闭，稳定 CI context 为 `Candidate Quality`。远程 Git 身份和 CI 结果只作治理与来源追溯，不能替代 `checker.source`、payload acceptance 或独立 proof。
- 首个目标领域：有键有限表的确定性转换；核心纯且无外部副作用，首批基准覆盖净额计算、键连接、守恒聚合和敏感字段非干扰。
- 首域语义：受界值、闭合记录、公开主键、无序有限表、显式缺失、无回绕算术、恰好一次连接、守恒聚合、关系非干扰和核心效果 `∅`。
- Axiom IR v0.1：严格版本化的 canonical JSON、内容寻址 DAG、无名称绑定、稳定摘要、无损 pretty 投影、结构化差异和显式迁移；未知字段与版本严格拒绝。
- Axiom Evidence v0.1：canonical JSON 证据清单、内容寻址义务、五种不可互换状态、可重放反例、显式 trust / uncovered、确定性结论聚合和独立义务重建；生产报告不能自证。
- 有键有限表基准语料 v0.1：四个任务各有一个正确候选、两个错误候选、基础 / 边界 / 无效输入、黄金输出，以及正确、错误、后端超时和输入拒绝场景的 Expected Evidence 断言；生成结果与摘要可离线重现。
- Agent 表示与验证反馈对比实验预注册 v0.1：固定 SQL、普通 JSON plan 和 Axiom projection 三种表示，两个模型条件、72 个 trial bundle、配对反馈、确认阈值、预算与停止线；正式调用仍须 execution lock 和单独授权。
- 版本身份：项目发布采用 `YY.M.RELEASE` CalVer 与 `dev` / `test` / `release` 轨道；语言语义、Axiom IR、Axiom Evidence 和工具实现分别标识，CalVer 不表达兼容性。
- `raxc` 生产编译器实现语言：Rust 2024 edition 与精确固定的 Rust `1.97.1` stable 工具链；该选择不适用于独立 checker，也不冻结表面语法或目标运行时。
- 首个验证后端：cvc5 1.3.4 独立 CLI；首版使用受容量约束的量化词自由 QF_UFLIA 类编码，优先评估 Alethe certificate，保留透明 `backend-attestation`，model 必须重放为规范反例，`unknown` / 超时 / 资源耗尽 / 协议错误失败关闭；不通过 FFI 或 SDK 链接进 `raxc`。
- 首个目标执行路径：从 canonical Axiom IR 确定性生成受限 ECMAScript ES module，由 Node.js 24.19.0 LTS 独立进程一次执行一个制品；语义整数只用 `BigInt`、文本拒绝未配对 surrogate、表输出按规范键排序，禁止 npm、动态代码和隐式宿主能力；production generator、host runtime 与 codec trust 保持可见，宿主语义差异映射为 `implementation_inconsistent`，不可归因的操作失败保持 `inconclusive`。
- 首版编译管线：`raxc-keyed-finite-table-pipeline-v0.1` 内容寻址制品 DAG；固定 IR 规范化、完整义务、单义务 query / cvc5 attempt、反例重放、输入检查、验证门控、Node target、宿主比较与 Evidence 装配顺序；缓存只复用精确身份的不可变制品，partial failure 进入非证明性 receipt，核心 `failed` / `unknown` 与输入拒绝均阻断目标生成和执行。
- 独立 checker：Go 1.26 语言基线与 `go1.26.7` 精确工具链；与生产 Rust `raxc` 分仓、分依赖图、分发布流水线和分进程，禁止复用生产 parser、normalizer、义务生成器、解释器或 adapter；以只读内容寻址 bundle 离线交换制品，独立结果在 Evidence 外区分 `accepted`、`accepted-with-trust`、`incomplete` 和 `rejected`，certificate / backend attestation、剩余 trust、资源失败与 checker 自身可信基保持可见。
- 独立 checker 首个实现切片：单独 Git 仓库与 Go module `radishaxiom.dev/independent-checker-go`，初始提交身份为 `edc55e3c37e7106d18a8046b0a289a6d6c354035`；只使用 Go 标准库，自有字节级 JSON/JCS 层拒绝重复 member、非法 UTF-8、非规范字节与未知字段，request / manifest / profile / limit set 闭合解析，bundle 只读核对普通文件、路径别名、长度、SHA-256 与 request 绑定。
- 独立 checker 源码快照身份 v0.1：闭合输入为除根 `.git` metadata 和 manifest 自身 sidecar 外的全部非目录 entry；路径限制为可移植 ASCII 组件并按 UTF-8 原始字节排序，只接受 `0644` / `0755` 普通文件，记录原始 byte length 与 SHA-256，并精确锁定 module、Go 语言基线和 toolchain 声明。当前 canonical manifest 覆盖 703 个文件、187,178 bytes，当前 `checker.source` 为 `sha256:401158c3c304f45faebebe879edf064512998423d7b08aec486f4be0012e3999`。最近可取得的 macOS arm64 候选 binary `sha256:02bdc09e3628a61364c5f5638245b9c5b745f20246ab34ff5d278503bb73a2e5` 精确绑定旧 `checker.source = sha256:e2c4ae31...1199044`；治理前更早的候选仍绑定 `sha256:3b809ba6...00d90b4` 且原始字节已删除。两者都不能冒充当前 source 的 artifact；Git commit / tree 也只作来源追溯，不能替代 source、binary 或 active registration identity。
- 独立 checker Axiom IR v0.1 严格结构与类型良构 profile：先由 bundle 层核对 raw content SHA-256，再由自有严格 JSON/JCS 层解析 IR；当前闭合接受 28 个 bundle 实际使用的 4 类 value type、5 类 node、18 类 expression、2 类 contract 与 2 类 aggregate tag，核对全部 definition domain ID、声明 / 接口引用、节点 DAG / 可达性和完整文档 domain digest。enum / record / table 独立索引同时核对 field 类型 / 标签与 primary key；expression 类型器核对无名称环境、字段、操作数、分支、table binder 和 Bool 顶层；node 关系检查核对 filter、projection、join pair、group key / aggregate、主键与 capacity。成功仍只返回身份与顶层计数，不形成 obligation、Evidence 状态语义或四态结果。
- 独立 checker Axiom Evidence v0.1 严格结构与身份 profile：bundle 层先行核对 raw content SHA-256 和资源边界，再由 checker 自有 strict JSON/JCS 与 SHA-256 解析 28 个锁定场景实际使用的 13 个顶层 member、闭合 subject / profile / entry / result / support / counterexample / trust / uncovered / conclusion tag；独立重算 tool、execution、obligation、trust、uncovered definition ID 和完整 Evidence document domain digest，建立 artifact / tool / execution / obligation / trust / IR document 的直接引用索引，并分别绑定 Axiom IR raw content 与 document domain digest。26 个身份有效场景覆盖 25 份唯一 Evidence 与 12 份唯一 IR；其中 `chk-bundle-01` 保留一个 manifest 已列出但缺失的非主体 artifact 进入 result `incomplete`，成功解析本身仍只返回身份与顶层计数，不判断 obligation 完整性、五态或 conclusion。
- 独立 checker obligation completeness profile：Axiom IR parser 保留按规范 ID 顺序形成的 node / contract definition、输入 / 输出接口与输出字段，由 checker 自身生成 document / program、非 input node、numeric expression / aggregate、row coverage / group conservation、field origin、guarantee / noninterference 静态义务；Evidence profile、显式 `host-input` / `host-output` / `golden-output` execution I/O 边界和 trust entry 只决定对应 benchmark / trust definition。所有 definition 用 checker 自有 canonical encoder 与 `axiom-evidence-v0.1:obligation` 域重算 ID，不读取生产 `axiom-obligation-set` 作为真相。25 个身份有效场景中 24 个集合精确匹配，`chk-obligation-01` 以缺少 `numeric-range` 得到 `obligation-mismatch`；成功仍不判断五态、support、replay 或 conclusion。
- 独立 checker state / support profile：按 obligation expectation 精确限制 `proved`、`checked`、`unknown`、`failed` 与 `trusted`，逐项检查 execution kind / result、tool role、attempt reason、artifact I/O 闭包、proof query / response、backend trust assumption 和 trust category；`replay-counterexample` 只接受 `counterexample-replayer`，不为旧 fixture 角色提供 alias。24 个 obligation-complete 场景通过，状态关系负例稳定得到 `invalid-state-support`；成功只确认声明边界自洽，不验证 kernel rule、backend attestation、反例或具体数据内容为真，也不重算 conclusion。
- 独立 checker counterexample world / `WF` profile：IR parser 保留 enum 声明顺序、闭合 record / table、input / output interface 与 assume contract ID，Evidence parser 保留 `failed` counterexample、precondition 与 world；`VerifyCounterexampleWorlds` 独立检查 world 数量、接口锚定、`bool` / `int` / `text` / `enum` 值、整数范围、闭合字段、容量、主键唯一性与规范顺序。24 个 state / support 完整场景通过；四个 input-conformance 失败场景允许呈现预期的非 `WF` / `Pre` 输入，其他失败反例必须 `WF`。成功仍不解析绑定 concrete artifact、不求值 `Pre` 或程序，也不验证目标违反、trace、公开等价、minimality 或 conclusion。
- 独立 checker concrete input artifact / `Pre` profile：bundle verifier 只重开 manifest 已登记制品并再次核对文件身份、长度与 raw SHA-256；checker 严格解析 12 份唯一 `axiom-benchmark-data` `0.1` host input，按 benchmark 与 IR input interface 重建完整类型化 world，独立检查 role、闭合 table / row / field、scalar、范围、容量、主键与规范顺序。assume 求值只闭合锁定语料实际使用的 field / bound、Bool / Int、`le`、`forall_rows`、`lookup` 与 `match_option` 子集，并受步骤和逻辑内存限制；24 个 state / support / world 场景全部通过，四个 `invalid-input` 分别得到两个 `Pre` 失败和两个 `WF` 失败，Evidence witness world 必须与 artifact 投影一致。成功仍不执行 transform node，也不验证非输入目标违反、host / golden output、proof、conclusion 或 result。
- 独立 checker finite IR execution / counterexample target replay profile：复用 concrete input 的类型化 value / record / table、无名称表达式环境、相等与范围语义，闭合执行锁定语料实际使用的 5 类 node、18 类 expression 和 2 类 aggregate；按稳定 DFS 拓扑顺序执行并在每个节点后重检 table 声明、容量、键唯一性、规范顺序和整数范围，步骤、逻辑内存、非唯一 lookup / join 与语义范围失败均失败关闭。`VerifyCounterexampleTargets` 独立绑定 trace、observed、obligation 与 required field / key，重放 contract-guarantee、noninterference、key-cardinality、field-origin、row-coverage 和 group-conservation 六类实际目标；8 个 `prove + failed` proof target 全部得到真实违反，paired-input 非干扰按 IR 标签和受保护公开输出核对公开等价，9 个 input-conformance 条目继续由既有路径负责，3 个 host / output mismatch 条目由 concrete output profile 负责。成功不检查 minimality、proof support、conclusion 或 result。
- 独立 checker concrete output comparison profile：`axiom-benchmark-data` input / output 复用同一严格 envelope、scalar、record 与 table decoder，同时区分数据 envelope role 与 Evidence execution I/O role；output artifact 必须重建为完整 `WF` IR output world。checker 对相关 `host-input` 自身执行有限 IR，保持 semantic、host / actual 与 golden 三个来源；9 条相关 host execution 中 8 条正确路径满足 semantic = host，故障路径满足 semantic = golden ≠ actual，7 条 checked comparison 相等且 1 条 failed comparison 真实不等。该不等 comparison 的 3 个 failed entry 分别核对自身 subject、trace、observed actual / expected 与 input witness；unknown 不升级，生产 Node target 不执行。成功不检查 minimality、proof support、conclusion 或 result。
- 独立 checker proof support 真值与能力边界 profile：`InspectProofSupports` 重新打开并复算 obligation-set、QF_UFLIA query、cvc5 response 与 prover tool artifact 身份；生产 obligation-set 只能与已独立检查的 Evidence obligations、IR 双摘要、semantics 和 profile 精确比较，不能反向充当义务真相源。query 只核对闭合 ASCII SMT envelope、logic 与单次 status command，response 只核对精确 `sat` / `unsat` / `unknown` frame；`TargetInObligationSet` 与 `QueryTheoremVerified` 分开，后者在当前材料下恒为 false。24 个前置完整场景共 213 个 producer `proved` claim：65 个 backend attestation 精确绑定 execution、tool 和 `proof-backend` trust，148 个 `kernel-replay` 因 kernel rule / certificate 能力集合为空而全部形成 `missing-proof-material`；`certificate-required` 下另有 12 个 attestation 缺少证明材料，合计 160 个缺失项，独立证明数为 0。该审计成功不是独立接受结果，也不重算 conclusion。
- 独立 checker production conclusion recompute profile：Evidence parser 保留 producer kind / refs 但不把它们当真相源；`VerifyConclusion` 从已检查的 obligation definition / expectation / 五态与必需 execution 完成状态按 `structure_rejected`、`input_rejected`、`implementation_inconsistent`、`violated`、`inconclusive`、`satisfied` 优先级独立形成决定性 refs，并按 ref 域与值稳定比较。24 个前置完整场景的 kind / refs 全部精确匹配；kind、遗漏、多余、错类、错值、顺序、优先级和必需 execution 漂移稳定得到 `conclusion-mismatch`。producer `satisfied` 不消费 proof audit 的 0 个独立 proof、65 个 attestation 或 160 个缺失材料；成功仍不是四态独立 result。
- 独立 checker 四态内存结果 profile：`EvaluateVerifiedBundle` 重新打开 Evidence / IR，并将 strict parse、identity、subject、obligation reconstruction、state / support、counterexample / concrete replay、proof support、conclusion recompute 与 isolation report 物化为闭合 code / refs / outcome；check definition 使用 `axiom-independent-check-v0.1:check` 域形成稳定 ID，request 同时保留 raw content 与 `axiom-independent-check-v0.1:request` document digest。Evidence 全部 trust 条目按 ID 保守进入 remaining trust，未被 request 允许的类别与缺失 artifact 形成 `incomplete`；任一确定拒绝优先，其次 incomplete，再次允许且非空 trust 的 `accepted-with-trust`，只有完整十项检查且 trust 为空时才为 `accepted`。25 个进入结果层的锁定场景由实际 bundle / Evidence / IR findings 形成 22 / 2 / 1 分布；正确 `violated`、`input_rejected`、`implementation_inconsistent` 均可接受，`certificate-required` 的 12 项 attestation 仍 incomplete。内存边界绑定 Evidence / request 双摘要与 checker source / 实际 toolchain / 四类必需 TCB source，不生成 canonical companion，不把 source 冒充 binary artifact，也不读取 expected result 决定 outcome。
- 独立 checker canonical companion / invocation failure profile：`EncodeCompanion` 只有在调用方显式提供与 source-level 边界逐项匹配的 checker binary / runtime TCB artifact、精确 `go1.26.7` 和实现版本时，才将既有内存结果唯一编码为 `axiom-independent-check-result` `0.1`；`ParseCompanion` 严格重解析并独立重算 check ID、四态顺位、ref 存在性、必需 check 集合与 `axiom-independent-check-v0.1:result` document digest。严格拒绝黄金 result 精确匹配 1,685 bytes 与双摘要，27 份指定 companion 全部可解析，25 个 actual result 全部 round-trip；source / binary / TCB 不可互换。外层进程失败另以 `axiom-checker-invocation-failure` `0.1` 绑定 canonical request 双身份和 `not-produced`，不能伪装四态；当前 macOS arm64 `0.1-dev` binary 已验收、持久发布并登记为 inactive，但在产品 launcher 完成安装后 qualification 与外部保存前仍不形成正式产品 runtime companion。
- 独立 checker invocation / 累计资源 profile：`InvokeBundle` 为一次调用建立唯一 `resourcebudget.Ledger`，累计 strict JSON token / item / depth、唯一 manifest artifact bytes、64 KiB digest block、义务 / 状态 / counterexample / concrete / proof / conclusion semantic step 与仍存活预算对象的 logical working memory；十类 check 前后、每 1,024 steps 与编码前执行 monotonic wall-clock 门禁。request / Evidence / IR 与 checker 身份可绑定后，`CHK-DIGEST-01` / `CHK-RESOURCE-01` 分别形成真实 `rejected` / `incomplete`，并存时拒绝优先；`CHK-PROCESS-01`、request 不可读 / 非规范、checker 身份缺失与输出截断继续只允许外层 failure / no-result，不由 expected companion 补造四态。
- 独立 checker CLI profile：唯一命令名为 `radishaxiom-independent-checker-go`，参数只接受精确 token `check` 与 `--bundle-root=<caller-mounted-readonly-canonical-realpath>`，stdin 必须为空；相对、冗余、symlink、缺失路径和额外参数在身份或 bundle 检查前失败。受控构建必须 linker 注入 `checker.source` 与非 `latest` 精确版本，运行时从 `runtime.Version()` 取得并强制匹配 `go1.26.7`，从 canonical `os.Executable()` 路径流式复算自身 SHA-256 作为 `checker.artifact` 和当前单体四类 runtime TCB artifact；任一身份漂移不得读取 bundle 或形成四态。成功 stdout 只有一个无尾随换行的 canonical result，stderr 为空；失败只产生不超过 64 KiB 的单行 UTF-8 诊断和非零退出，截断 stdout 继续由外层解释为 `not-produced`。
- 独立 checker macOS arm64 受控构建与 payload acceptance profile：唯一 builder 复算已接受的 `go1.26.7.darwin-arm64.tar.gz` 原始身份、安全解包并核对 `16,701` member / `228,748,173` regular bytes、`VERSION` 和实际 `go version`；两次构建分别使用隔离 cache / module cache / GOPATH / HOME / TMP、空 PATH、关闭网络 / toolchain 自动下载 / telemetry / workspace，固定 darwin / arm64 / v8.0、`-trimpath`、`-buildvcs=false`、空 build ID 和 linker source / version。每次构建后和最终物化前重新核对 source sidecar，只有 binary 原始字节一致才输出 artifact 与 canonical provenance。独立 acceptance 不调用 builder / compiler，从原始 binary 检查 64-bit arm64 Mach-O、闭合 Go build info、module / setting、provenance 和三条真实 CLI 结果后形成 canonical acceptance；该决定只允许进入受控登记候选，不自动安装、发布或扩展到其他平台。
- 独立 checker runtime launcher policy：ADR 0011 / 0012 与 `radishaxiom-checker-runtime-launcher-policy` `0.2` 将产品选择限制为恰好一个 active 记录和 `darwin / arm64 / v8.0 / macho-64-arm64` 精确目标，拒绝 Rosetta 跨选、`PATH` / latest / cache / 用户 executable fallback；安装使用精确 immutable asset、同文件系统 staging、严格两层 archive / binary 复核、content-addressed immutable slot 和 canonical installation receipt。installed-inactive 还必须以相同 launcher 边界重放 `ax-b01-correct`、`chk-digest-01`、`chk-resource-01`，保存现有 `axiom-independent-check-result` 三份正式 companion 与独立 qualification record；外层 process / identity failure 不形成四态。`0.2` 显式替代闭合但缺少宿主 / store 字段的 pre-implementation `0.1`，登记集合与既有 inactive record 仍保持 `v0.1` 字节不变；Rust 已实现 registry identity / target selection 前段，完整 policy 仍为 `specified-not-implemented`，installation / qualification / activation 均未物化。
- 产品侧 checker runtime 实现宿主：ADR 0012 将 launcher / installer core 冻结为主仓 Rust 2024、精确 `1.97.1` 生产图中的内部组件，与 `raxc` 共用 Cargo workspace、依赖治理和产品发布图；core 不持有网络 / provider credential，产品注入私有根和 `checker-runtime-store-v0.1` 六项持久化能力，qualification / invocation 共用单一 Rust result consumer 与 immutable spawn plan。只允许通过 canonical bytes 和离线 subprocess 调用独立 Go checker，禁止复用其源码 / parser，也禁止让 Python 一致性核心成为产品 runtime。当前单 crate workspace / lockfile、safe SHA-256、strict policy / current record identity、typed target 与 registration selection 已物化；绝对安装根、store、archive / receipt、result consumer、spawn、公开 CLI 和完整 launcher 仍未物化。
- Independent Check Contract v0.1：以 JSON Schema Draft 2020-12 描述 request / bundle manifest / result 抽象结构，并由独立生成器固定 JCS 字节、域摘要、check ID、闭合 code registry、四态聚合、一个严格 Evidence 拒绝 bundle 和 18 个结构 / 顺序 / 身份负例；当前只覆盖 ASCII fixture，不冒充完整 Unicode / JCS、Evidence 语义或 checker 实现。
- Toolchain & Adapter Identity Registry v0.1：固定 Rust `1.97.1`、Go `go1.26.7`、cvc5 `1.3.4`、Node.js `24.19.0` 的 source 与 Linux / macOS / Windows `amd64` / `arm64` 候选制品，登记官方来源、publisher 摘要、依赖审阅目标、许可证来源以及七个 build / adapter / target / invocation / pipeline / checker profile；Go macOS arm64 host/source 两项绑定版本化 acceptance record。Rust 另以 rustup distribution 绑定 `2026-07-16` channel manifest、`minimal` profile、macOS arm64 target、五个 component 与 source acceptance；standalone 和其他平台仍未接受，其余 Rust 平台制品与 cvc5 source 摘要仍待权威元数据捕获。
- Toolchain Payload Acceptance v0.1：Go `go1.26.7` macOS arm64 host/source 与 Rust `1.97.1` macOS arm64 五个 rustup component/source 共八项精确 payload 的项目重算 SHA-256 均匹配 publisher 记录。Go host/source 的只读布局、许可证/专利、vendor manifest 与 17 个模块一致；Rust component 无 link / hardlink / 特殊文件，source 的 323,914 个 member、148 个受控 symlink、4,613 份 `Cargo.toml`、1,977 份 `Cargo.lock` 和两套各 12 种 SPDX 表达式已记录。八项仅接受为 `accepted-for-controlled-build-input`，签名保持 `not-verified-no-signature-input`，不授权安装、执行或具体分发法律结论。
- Execution Profile Contract v0.1：以单一 canonical manifest 固定 cvc5 `1.3.4` QF_UFLIA、Node.js `24.19.0` invocation 与 Go checker 的允许参数、清空环境、stdin / stdout framing、内部与外层资源边界、checker 七项确定性计数和结果形成停止线；Alethe / CPC 仅列为候选，受支持 certificate profile 集合为 0，proof / attestation 结论反向引用既有权威场景而不复制第二套 outcome、trust 或 code。
- Pipeline Artifact Contract v0.1：以三个 JSON Schema 和两个原始文本 profile 固定 obligation set、host data、SMT query、target module 与 pipeline receipt 的首批结构和规范字节；包含 gate 打开的完整 receipt、cvc5 timeout 后阻断 P6–P8 的 partial receipt、域摘要 / tool / cache 身份及 38 个关键负例。当前只覆盖 ASCII 合成 fixture 和 AX-B01 最小结构切片，不冒充义务完整性、parser、solver、target 执行、Evidence 或跨平台结果。
- Implementation Readiness Contract v0.1：以 canonical manifest 和 JSON Schema 统一 20 个 benchmark、ADR 0008 的 16 个 `CHK-*` 及 10 个 pipeline / readiness 场景，逐行固定输入、P0–P9、gate、artifact role、receipt、Evidence、独立结果 / 进程、trust / uncovered 与六平台注册适用范围；59 条 benchmark / ADR 来源要求全部具有反向场景覆盖，并以 13 个负例拒绝 gate 绕过、义务遗漏、artifact 篡改、伪造 cache hit、缺失 bundle 后错误接受、attestation 越权、错误结论聚合、进程失败伪装结果及未知 member / version / profile。全部场景保持 `specified`，`observed` 为 0。
- Keyed Finite Table Checker Bundle Contract v0.1：从 readiness 的稳定 ID 物化 20 个 benchmark、5 个跨契约场景和 3 个负例；每个目录仅含 request、manifest 与内容寻址 blob，完整绑定 IR、义务、pipeline receipt、Axiom Evidence、独立预期结果及 10 类 check ID。fixture tool 已按既有 v0.1 角色精确声明 `counterexample-replayer`，生成器会拒绝 execution kind / tool role 错配；当前 `contract.json` 原始 SHA-256 为 `a349152cb2f838cf5acfaa66ef1676554f6d5f453d07314b3cc1b8c5579c7974`。正确 / 错误 / 非法输入 / timeout、host mismatch、certificate required / attestation allowed、checker 内部资源不足与进程外层失败均保持独立聚合；全部仍为 `specified`，不冒充 checker、solver、Node 或六平台执行。
- 许可证：Apache License 2.0，并形成开放基础层与商业化边界策略。
- 仓库治理：`master` 稳定主线、`dev` 日常集成、PR 门禁和合并后回流策略。
- 当前仓库级验证：`./scripts/check-repo.sh` 或 `pwsh ./scripts/check-repo.ps1`。

## 前序进展（截至 2026-08-24）

1. 盘点并冻结 28 个 checker bundle 实际使用的 Axiom Evidence v0.1 闭合 profile：13 个顶层 member、2 个 obligation profile、tool / execution / obligation / trust / uncovered definition domain、五种 result、两种 proof support、五种 counterexample、直接引用目标与确定性数组顺序；范围外组合继续失败关闭。
2. 在独立 checker 新增零第三方依赖 `internal/axiomevidence`，只使用 checker 自有 `strictjson`、canonical bytes、SHA-256 与闭合 tagged union；没有导入、复制或生成生产 Evidence parser、聚合器、义务生成器、反例重放器或测试 helper。
3. parser 逐项重算 tool、execution、obligation、trust、uncovered definition ID 和完整 Evidence document domain digest；obligation `result` 不进入 obligation ID，map 只用于摘要查找，不参与规范输出、摘要或拒绝顺序。
4. 建立 artifact、tool、execution、obligation、trust 与 IR document 的直接引用索引，拒绝悬空 producer / artifact / execution / obligation / trust、歧义 conclusion ref、错误 producer role 和 subject 外 IR document；subject artifact 必须解析到 `axiom-ir` `0.1`，并分别绑定独立 IR parser 的 raw content 与 document domain digest。
5. counterexample 当前只闭合解析锁定语料的五种 kind、`reduced` minimality、document / obligation / observation trace、world / table / record 结构、`enum` / `int` / `text` value 和两种 observed；不重放 world、`WF` / `Pre`、失败义务、公开等价或最小性。
6. 25 个身份有效场景全部完成 Evidence raw / document identity 与 IR subject 双摘要核对，覆盖 25 份唯一 Evidence 和 12 份唯一 IR；`chk-bundle-01`、`chk-digest-01`、`chk-resource-01` 继续在 artifact missing、raw digest、resource limit 层先行拒绝。
7. 新增未知顶层 member / version / support tag、非规范 artifact / conclusion ref 顺序、definition ID 漂移、悬空 producer、错误 subject artifact、Evidence document digest 与 IR subject 绑定不匹配负例；结构成功不判断 expectation / 五态、support 语义、obligation 完整性或 conclusion 正确性。
8. 新增 `docs/axiom-evidence-structure-v0.1.md` 并更新 checker README，明确锁定 artifact 清单中 options / policy 可由 receipt 间接绑定；当前 parser 只保证直接引用闭合，未读取 receipt，也不冒充 artifact graph 最终消费检查。
9. `checker.source` 重放为 619 个文件、170,987 bytes manifest 与 `sha256:45084c7cb2f4d0834fd3c8b1bd760140bc821fef5d280dffe6d3e0ce8cd2c8ca`。以本机 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 运行源码身份专项门禁、全量 `go test -count=1 ./...`、`go vet ./...`、脚本语法与 `go list -m all`，全部通过且 module 集合只有 checker 自身；这不是精确 `go1.26.7` 或六平台证据。
10. 本切片没有安装依赖、执行 solver / Node / 生产编译器、构建 binary、生成 `checker.artifact` / 独立 result、创建 remote、push、发布或部署。checker 实现提交为 `2aaa3ab02ff8203155e2d156727478d3da086a85`、tree 为 `e879b69f184fc7b28dad0017ea25c274beaff820`；Git 身份只作来源追溯，不替代 `checker.source` 或协议摘要。
11. 从首域语义、Axiom IR、Axiom Evidence 与 12 份唯一 IR 独立盘点 verification / benchmark 义务生成位置；node、contract、expression path、interface、field、artifact 与 trust anchor 均由 checker 自有模型表达，没有读取、解析或导入生产 `axiom-obligation-set` 来形成期待集合。
12. Axiom IR parser 现在保留 canonical node / contract definition 与确定性接口 / 字段投影，生成 `ir-structure`、`effect-empty`、非 input node 的 `totality` / `key-cardinality`、四类转换的 `row-coverage`、group `group-conservation`、numeric expression / aggregate path、output field origin 和 contract 义务；group 覆盖与守恒保持两项不可合并义务。
13. Evidence parser 将 obligation definition 解码为结果无关的闭合 anchor union，并保留 profile、execution I/O 与 trust category。benchmark 的具体义务只从唯一 `host-input`、`host-output`、`golden-output` role 和 IR interface 生成；role 与 execution / tool / result 的语义配对明确留给下一切片。
14. `VerifyObligationCompleteness` 对每个独立 definition 重放 canonical bytes 与 obligation domain ID，再与 Evidence 集合精确比较；缺失、多余、错误 expectation、kind、anchor、path 和重复 definition 统一得到新登记的 `obligation-mismatch`，map 不参与规范输出或拒绝顺序。
15. 28 个锁定 bundle 中 24 个身份有效且非 obligation-negative 场景通过完整性比较，`chk-obligation-01` 从结构有效推进为缺少一个规范 `numeric-range` 的明确拒绝；`chk-bundle-01`、`chk-digest-01`、`chk-resource-01` 继续在 artifact missing、raw digest、resource limit 前置层拒绝。
16. 新增多余义务、expectation / path / anchor 漂移、同 anchor 冲突 expectation、非规范 obligation 顺序和 100 次重建稳定性负例；结构 / 身份正负例、12 份唯一 IR 及原有 bundle 门禁保持原结论。
17. 新增 `docs/axiom-evidence-obligation-completeness-v0.1.md` 并同步 checker README / 结构说明，明确 trust entry 一一对应不等于真实依赖已经完备，benchmark I/O role 只形成义务边界而不提前冒充 state / support 或 concrete replay。
18. `checker.source` 重放为 623 个文件、171,771 bytes manifest 与 `sha256:309aec52bdea5f5049502e408e08172185f742de56aaf3f7a1769867d25e5bac`。在真实 checker 以本机 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 运行全量 `go test -count=1 ./...`、`go vet ./...`、源码身份门禁、脚本语法与 `go list -m all`，全部通过且 module 集合只有 checker 自身；实现提交为 `57f7431480a4f49faaba7cfc2b5fcec975a0edaa`、tree 为 `5ba0eb3c000bef599f7c33959b9e8769212eb961`。这不是精确 `go1.26.7` 或六平台证据。
19. 复核时确认 Axiom Evidence v0.1 的“义务生成位置”表没有显式列出 group `row-coverage`，而首域语义、Axiom IR AX-B03 映射和既有锁定 bundle 一致要求 group 同时具有覆盖与守恒义务。为避免原地改写已被摘要 DAG 绑定的公共规范，本切片只对锁定 profile 声明支持并记录该文档漂移；扩大 profile 或冻结新 Evidence 版本前必须以版本化修正和摘要迁移收口。
20. 截至 obligation completeness 子切片没有安装依赖、修改公共格式字节或重生成锁定 bundle，没有执行 solver / Node / 生产编译器、构建 binary、生成 `checker.artifact` / 独立 result、创建 remote、push、发布或部署。
21. 接受 [ADR 0009](../adr/0009-axiom-evidence-v0-drift-and-migration.md)：Axiom Evidence v0.1 原始规范字节保持不变，group `row-coverage` 与 execution kind / tool role 的公共修正在 v0.2 显式版本化；v0.1 → v0.2 必须重建 obligation、execution / tool、Evidence 与独立结果摘要，不跨版本复用 `proved`、`checked` 或 `trusted`。
22. 修正 checker bundle generator：形成 `replay-counterexample` execution 的 fixture tool 增加既有 `counterexample-replayer` role，生成期按全部 execution kind 检查精确 tool role；checker 不接受 `fixture-checker` alias 或默认成功 fallback。
23. 按完整内容寻址 DAG 重生成 558 个 bundle contract 文件并精确导入 checker；28 个场景的结构语义与 `specified` 状态未变，`contract.json` 原始 SHA-256 更新为 `a349152cb2f838cf5acfaa66ef1676554f6d5f453d07314b3cc1b8c5579c7974`。这次摘要迁移只修正无效 fixture 角色，不是 solver、Node、checker 或六平台运行观察。
24. Evidence parser 现在保留 artifact、tool、execution、obligation result、proof support、counterexample、attempt 与 trust 的状态关系输入；state / support validator 按稳定 ID 顺序检查 expectation / 五态矩阵、execution 完成状态、tool role、attempt reason、artifact I/O 闭包、query / response、backend trust assumption 和 trust category。
25. `proved` 只接受当前 profile 的 completed `prove` 边界及 `kernel-replay` / `backend-attestation` 精确引用，`checked` 按义务种类绑定 `check-fixture` / `execute-host` / `compare-output` 与制品闭包，`unknown` 的 reason 与 attempt result 必须对应，`failed` 只接受对应 completed replay / compare 边界，`trusted` 只完成同一 trust 的 `trust-boundary`。
26. 24 个 obligation-complete 场景逐项通过 state / support；`chk-bundle-01`、`chk-digest-01`、`chk-resource-01` 与 `chk-obligation-01` 继续保持前置拒绝优先级。新增 expectation / state 错配、缺失 support、错误 tool role、execution 未完成、attempt reason 漂移、attestation trust 缺失、response 漂移、checked artifact 遗漏、trusted scope 错配和 failed execution kind 错配 10 类负例，以及 100 次稳定重放。
27. 新增 checker `docs/axiom-evidence-state-support-v0.1.md`，并同步 README、Evidence 结构与 obligation completeness 说明；明确本切片只验证声明边界闭合，不检查 kernel rule、backend attestation、certificate、反例或 concrete data 真值，不重算 conclusion，也不形成独立四态 result。
28. `checker.source` 重放为 626 个文件、172,361 bytes manifest 与 `sha256:3a664b7158921a871bc9b4f39293b5b1017d09131190edfa93d6670a4a6ec211`。以本机 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 运行全量 `go test -count=1 ./...`、`go vet ./...`、源码身份门禁、脚本语法、`go list -m all`、`gofmt -l .` 与差异卫生检查，全部通过且 module 集合只有 checker 自身；这不是精确 `go1.26.7` 或六平台证据。
29. 本 state / support 子切片没有安装依赖、执行 solver / Node / 生产编译器、构建 binary、生成 `checker.artifact` / 独立 result、创建 remote、push、发布或部署。checker 实现提交为 `f273ca3a0768fea96637771269f63582f0e70ce8`、tree 为 `bce5bb22725b0e6e184cc1f317296d626ab75f0e`；Git 身份只作来源追溯，不替代 `checker.source` 或协议摘要。
30. 盘点 24 个 state / support 完整场景中的 20 个 `failed` counterexample：覆盖 `single-row`、`row-pair`、`paired-input`、`missing-key` 与 `group`，实际值包含 enum、int 与 text；同时保留规范要求的 bool 支持。四个 invalid-input 场景的 witness 本来就可能违反 `WF` 或 `Pre`，不能与核心、宿主或输出失败反例使用同一“必须良构”规则。
31. Axiom IR parser 现在保留 enum member 声明顺序、record / table 定义、input / output table map 与 assume contract ID；具体数据模型以同一组类型定义表达 value、record、table 与 world，不另建绕过 IR 声明的 schema 口径。
32. Axiom Evidence parser 不再丢弃 `failed` counterexample、precondition 或 world，并补齐 `bool` value；复制与完整性路径保留完整 counterexample，避免状态检查后丢失重放输入。
33. 新增 `VerifyCounterexampleWorlds`：先绑定 Evidence subject IR，再按稳定 obligation ID 检查 counterexample kind 对应的 world 数量、precondition 是否来自 IR assume，以及每个 world 的接口、record / field、值类型与范围、enum 名义类型 / member、table capacity、主键唯一性和 canonical key order。除 input-conformance 失败外，反例 world 必须 `WF`；拒绝统一登记为 `counterexample-invalid`。
34. 24 个 obligation-complete / state-support 场景继续通过 counterexample world / `WF` 检查，四个 invalid-input 场景继续以其预期的非良构或 `Pre` 违反输入通过该层；`chk-bundle-01`、`chk-digest-01`、`chk-resource-01` 与 `chk-obligation-01` 保持更早拒绝优先级。
35. 新增 world 数量、未知接口、整数越界、未知 enum member、字段缺失、record 类型漂移、重复主键、缺少完整 assume 集和非 assume precondition 九类拒绝；合成 IR 测试另覆盖 bool / int / text / enum、enum 声明序主键、容量、逆序键、重复键和值 kind 漂移，并完成 100 次稳定重放。
36. 新增 checker `docs/axiom-evidence-counterexample-worlds-v0.1.md`，同步 README 与 Evidence 结构说明；明确当前 world 是 Evidence 内嵌有限投影，完整 host input 仍须在下一切片从绑定 artifact 重建，不能用部分 world 自证输入完整性或 `Pre`。
37. `checker.source` 重放为 631 个文件、173,357 bytes manifest 与 `sha256:68c589adf8a304398c9aec993622c62eb77c1af5bd83d7a5019b0fb0a8fdafe4`。以本机 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 运行全量 `go test -count=1 ./...`、`go vet ./...`、源码身份门禁、脚本语法、`go list -m all`、`gofmt -l .` 与差异卫生检查，全部通过且 module 集合只有 checker 自身；这不是精确 `go1.26.7` 或六平台证据。
38. 本 counterexample world / `WF` 子切片没有安装依赖、执行 solver / Node / 生产编译器、构建 binary、生成 `checker.artifact` / 独立 result、创建 remote、push、发布或部署。checker 实现提交为 `517245714cd20436f7dcb7bec6f33020b43ec807`、tree 为 `9ce28e9f74019e0cfedf101351c8e53934e53e86`；Git 身份只作来源追溯，不替代 `checker.source` 或协议摘要，主仓库只同步状态文档与索引。
39. 从 24 个 state / support / world 完整场景盘点出 12 份唯一 host-input artifact 和 12 份唯一 IR；artifact role 分为 8 份 `input` 与 4 份 `invalid-input`。assume 实际子集只出现在 AX-B01 / B02，分别覆盖逐行整数上界与主键 lookup 存在性；AX-B03 / B04 的 invalid 输入由主键重复直接违反 `WF`。
40. checker strict JSON 增加面向外部数据制品的严格 document 入口：允许合法 JSON 空白、member 重排和 escape，但继续拒绝 duplicate member、BOM、非法 UTF-8、number / `null` 与资源超限；canonical request、manifest、IR 与 Evidence 入口保持原字节级要求。bundle verifier 增加 manifest 内 artifact 的受约束重开，重读时再次核对文件身份、长度与 raw SHA-256，防止前置验证后的路径或内容漂移。
41. 新增 `axiomir` concrete benchmark data 解码、完整 input world 重建、`WF` 与 assume / `Pre` 求值，并由 `axiomevidence.VerifyConcreteInputs` 将 execution 的全部 host-input 集、artifact 声明、benchmark、IR、obligation result 和 counterexample witness 严格绑定。`checked` 只接受 `input` 且 `WF ∧ Pre`；`failed` 只接受 `invalid-input` 且具有真实 `WF` 或 `Pre` 失败，关系不成立稳定得到 `concrete-check-mismatch`。
42. 24 个 state / support / world 完整场景全部进入 concrete input 检查，12 份唯一 artifact 完整重建；四个 invalid 场景稳定分类为 AX-B01 / B02 的 `Pre` 失败与 AX-B03 / B04 的 `WF` 失败。负例覆盖 artifact format / version / role / execution 绑定、digest、缺失或多余 table / field、scalar / enum、容量 / 主键顺序、world 投影、逻辑内存和步骤限制，并对 B02 invalid 完成 100 次稳定重放。
43. `checker.source` 随本切片和日终文档澄清重放为 637 个文件、174,521 bytes manifest 与 `sha256:cb892c672816b99f6e4d7a1c09ea0374c0f21e574fe2907fa99e9fae5cf5be92`。以本机 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 运行全量 `go test -count=1 ./...`、`go vet ./...`、源码身份门禁、脚本语法与 `go list -m all`，全部通过且 module 集合只有 checker 自身；这不是精确 `go1.26.7` 或六平台证据。
44. 本 concrete input / `Pre` 子切片没有安装依赖、执行 solver / Node / 生产编译器、构建 binary、生成 `checker.artifact` / 独立 result、创建 remote、push、发布或部署。主仓库只形成一次日终文档提交；checker 的实现、说明与源码身份继续作为同一未提交切片保留，等待明日先行审阅和提交。Git 工作树或提交状态不替代 `checker.source` 或协议摘要。
45. 日终按顺序复核主仓库与 checker 今日各 4 个已提交变更及当前 concrete input / `Pre` 差异，确认 ADR 0009、锁定 bundle 角色迁移、Evidence 分层实现和主状态一致；早期 checker state / support 与 obligation completeness 说明补充后续 world / concrete 层入口，concrete input 说明澄清 strict JSON parser 与 profile decoder 的职责。21 台 UTM 虚拟机均已确认停止，没有删除虚拟机或快照。

## 昨日进展（2026-08-23）

1. 将原 2063 行的 Pipeline Artifact Contract 生成器按公共编码、fixture 构造、校验、负例、Schema 和输出编排拆分为私有模块，保留原命令入口；拆分后 53 个生成文件及 contract 登记的 52 个原始字节摘要未变化。
2. 建立 `contracts/implementation-readiness-v0.1/` 与零第三方依赖生成入口，原始摘要绑定四题语料、首域语义、Axiom IR / Evidence、Independent Check Contract、Pipeline Artifact Contract、工具身份注册表及 ADR 0007 / 0008。
3. 物化 46 行 canonical 场景矩阵和 59 条来源覆盖：20 个 benchmark、16 个 `CHK-*`、10 个 pipeline / readiness 路径；重叠验收要求只通过 `source_refs` / `coverage` 映射，不复制第二套结果。
4. 每行固定 P0–P9、gate、artifact role、receipt、Evidence、独立 result / process、trust / uncovered 和平台注册适用范围；全部为 `specified`，`observed_scenarios` 为 0。wrong、`unknown`、invalid 及 P0 工具未验收路径明确禁止 target module、host output 与 `execute-host`。
5. 同批生成 JSON Schema、canonical manifest、域摘要、原始字节摘要及 13 个非法组合负例；Pipeline 专项、Readiness 专项和仓库级检查均通过。
6. 建立 `contracts/keyed-finite-table-checker-bundles-v0.1/` 与零第三方依赖生成入口，从 readiness manifest 的稳定场景 ID 生成 20 个 benchmark bundle、5 个跨契约 bundle 和 3 个缺失 / 篡改 / 省略负例。
7. 为四题 canonical IR 重建完整 benchmark obligation set，逐项物化节点总性 / 键基数、算术与聚合范围、row coverage、输出字段来源、guarantee / noninterference、具体输入、宿主输出、黄金比较及 trust-boundary；Evidence 状态、反例 world、attempt、receipt 和结论保持摘要闭合。
8. `CHK-CONCRETE-01` 将 host mismatch 保持为 `implementation_inconsistent`，`CHK-PROOF-01` / `02` 分别固定 certificate-required 的 `incomplete` 与 attestation-allowed 的 `accepted-with-trust`，`CHK-RESOURCE-01` 使用请求内 step budget 形成 `incomplete`，`CHK-PROCESS-01` 只生成外层进程失败而不伪造四态结果。
9. 每个 bundle 只物化 manifest 列出的普通 blob；request、manifest、Evidence、receipt、独立结果、条目和 check ID 均可离线重算，预期结果保持在输入 bundle 之外。readiness 的 20 个 benchmark 行已从 `specified-not-materialized` 收口为 `complete`，证据等级仍为 `specified`。
10. Checker Bundle、Independent Check、Pipeline Artifact、Implementation Readiness、Toolchain Registry、Benchmark Corpus 专项生成检查、JSON Schema Draft 2020-12 校验和仓库级 776 文件检查均已通过。
11. 建立 `contracts/execution-profiles-v0.1/` 与零第三方依赖生成入口；单一 manifest 物化 3 个执行 profile、5 个职责分离 limit set、5 个 canonical 投影、27 个逐字段 / 边界负例和来源摘要。
12. cvc5 profile 固定 safe / strict / QF_UFLIA、model / proof 生产与内部检查、`rlimit-per` / `tlimit-per` 和外层 stream / wall / memory 上限，拒绝路径参数、环境继承、随机 seed 与未登记 option；Node profile 固定 Permission Model、目标模块摘要解析只读 grant、BigInt / UTF-8 codec、进程 / 输出限制和能力 denylist。
13. checker profile 为 request 七项 limit 固定 parser、摘要、IR / obligation、replay、certificate、wall-clock 与逻辑内存的计数位置；`CHK-RESOURCE-01` 只允许内部检测后形成规范结果，`CHK-PROCESS-01` 的外层 kill / crash / 截断只能形成进程失败记录。certificate 支持集合保持为空，`CHK-PROOF-01` / `02` 继续作为唯一结论权威。
14. cvc5 / Node registry profile 更新为 `specified-not-materialized`，不改变 payload 的 `not-accepted`；28 个 checker bundle 全部新增 Execution Profile manifest normative blob 与 contract 来源绑定，摘要链按 registry → pipeline → readiness → execution profiles → bundles 顺序重算。
15. Toolchain Registry、Pipeline Artifact、Implementation Readiness、Execution Profile、Checker Bundle 专项重放、3 份 JSON Schema Draft 2020-12 校验、28 个 bundle profile blob 绑定检查和仓库级 848 文件检查均通过；全部仍为 `specified`，没有产生工具运行观察。
16. 从 registry 登记的官方 HTTPS URL 下载 Go `go1.26.7` macOS arm64 host 与 source 到隔离临时目录；host 单连接中断后断点续传，source 低速连接停止后按官方 Range 能力补齐连续区间。两项最终字节数和项目重算 SHA-256 都与 publisher 记录完全一致，没有安装或执行归档内容。
17. 新增 `scripts/inspect-toolchain-tar.py`，不释放 archive 即拒绝绝对 / 非规范 / 越界 / 重复路径、越界链接、特殊文件和 setuid/setgid，记录全成员元数据摘要、布局、必需文件、许可证/专利与 vendor module 清单；host/source 分别观察 16,701 / 16,675 个成员，链接与特殊文件均为 0。
18. 建立 `contracts/toolchain-payload-acceptance-v0.1/`：两份脱离绝对路径的观察、两份逐 payload acceptance record、registry / inspector 摘要绑定、两份 Draft 2020-12 schema 和 7 个负例；Go host/source 的许可证与依赖清单一致，最终作用域明确排除安装、执行、签名验证、源码可复现性、checker 正确性、跨平台等价和具体分发法律判断。
19. Toolchain Registry、Payload Acceptance、Pipeline Artifact、Implementation Readiness、Execution Profile 与 Checker Bundle 的完整摘要 DAG 已按顺序重算和重放；两份新 schema 通过 Draft 2020-12 校验，仓库级 866 文件检查通过。128 MiB 隔离下载目录及其中 13 个归档、分段和临时检查文件已经精确删除。
20. 在主仓库之外建立独立 Git 仓库 `RadishAxiomChecker`，固定 Apache-2.0、`go 1.26.0`、`toolchain go1.26.7`、`GOTOOLCHAIN=local` / `CGO_ENABLED=0` 使用方式和零第三方依赖边界；没有建立 submodule、相对 module 依赖、remote 或发布流水线。
21. 实现自有字节级严格 JSON/JCS、request / manifest / digest / profile / limit set 闭合解析，以及 bundle 目录只读 resolver；拒绝重复 member、非法 UTF-8、非规范 JSON、未知字段 / 版本 / profile、路径别名、symlink、非普通文件、未列出或缺失 artifact、长度 / SHA-256 不符和 request 绑定不符。
22. 导入 Independent Check Contract 的正负例与 28 个指定态 checker bundle，提交来源锁和原始摘要；测试覆盖 25 个身份层可解析场景，以及 artifact missing、digest mismatch、resource limit 三个当前切片可判定的拒绝路径。完整 IR、obligation、Evidence、certificate、结果装配和累计资源核算保持未实现。
23. 将已验收的 Go `go1.26.7` macOS arm64 host 精确解包到隔离临时前缀，以 `GOTOOLCHAIN=local`、`CGO_ENABLED=0` 运行 `go test -count=1 ./...` 与 `go vet ./...`，两项通过；临时工具链、归档和缓存随后精确删除，本机默认 Go 仍为 `go1.26.3`，没有修改 `mise`、PATH 或全局配置。
24. checker 初始历史拆为仓库边界 `d889be26c60146011a8d2ded05327737d5a76003`、严格 parser `b37b7c3253c50c2f2ef18f12e7fb628169426cd2` 和 bundle 语料 `edc55e3c37e7106d18a8046b0a289a6d6c354035` 三个可审阅提交；当前工作树干净且没有 remote。Git tree `cc12e8bac7a1d9a75dc605184b60a46ce63128ef` 只用于 Git 内部追溯，不冒充协议 SHA-256 源码身份。
25. 在 checker 仓库冻结 `checker.source` v0.1：输入集合不读取 Git index / ignore / remote，除根 `.git` metadata 与精确 manifest sidecar 外覆盖全部非目录 entry；目录只用于遍历，路径使用可移植 ASCII 组件和 UTF-8 byte order，文件只接受普通 `0644` / `0755`，长度与 SHA-256 均覆盖未经文本或换行转换的原始字节。
26. 新增零第三方依赖 `internal/sourceidentity`、canonical manifest sidecar 和 check / update 门禁；595 个输入文件形成 166,489 bytes manifest 与 `checker.source` `sha256:587e6d68e90e703cae786d4e382f2b627fb9e3468f29e1d3831bd703bf70699c`。负例覆盖未跟踪文件、原始字节 / mode / 生成漂移、module / Go / toolchain 身份变化、非可移植路径、symlink、自引用和 Git worktree metadata 排除。
27. 以本机默认 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0` 和隔离临时 cache 运行源码身份专项门禁、`go test -count=1 ./...`、`go vet ./...`、脚本语法检查与 `GOPROXY=off go list -m all`，全部通过且 module 集合只有 checker 自身；这不是精确 `go1.26.7` 或六平台证据。提交 `f975e731cc3cdc236b4cffd542c9446724517396` 与 tree `799aff7601ad4c10c7b2047f7051aeea58552889` 只作来源追溯，checker 工作树干净且仍无 remote。
28. 在 checker 自有 `strictjson` 上增加从已接受 Value 重放 canonical bytes 的无 number / `null` 编码路径；Axiom IR parser 逐项闭合检查顶层、声明、节点、表达式、输出与契约，并用 6 个固定 domain 对 definition 与完整文档重算 SHA-256，不导入或复制生产 Rust parser / normalizer。
29. 28 个锁定 bundle 中 25 个身份有效场景进入 IR parser，覆盖 12 份唯一 IR；`chk-bundle-01`、`chk-digest-01`、`chk-resource-01` 继续分别在 artifact missing、raw content digest 和 resource limit 层先行拒绝。12 份 IR 的 document domain digest 全部与既有锁定外部记录一致，错误候选保持结构良构而未被误拒绝。
30. 新增未知 member / version / tag、语义数组非规范顺序、definition domain ID 漂移、悬空输出节点引用和 document domain digest 不匹配负例；完整类型推导、字段覆盖、键 / 容量 / 控制依赖、obligation、Evidence、certificate、result 与 CLI 均未进入本切片。
31. `checker.source` 随实现重放为 605 个文件、168,322 bytes manifest 与 `sha256:05a6f75f4802ffad7370441530e27b7dcb497e0e5575c41be73e149d630e457a`。以本机 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 运行全量 test / vet、源码身份门禁、脚本语法与 module 列表均通过；提交 `df65635d8bdecbb0513a7e2c6a881410c5ffcf90`、tree `37c1a29ec87c8ba4c1815b35f674c78bf2057e87` 只作来源追溯，checker 工作树干净、没有 remote，也没有精确 `go1.26.7` 或六平台新证据。
32. 将 Axiom IR parser 原来只作存在性查找的 enum / record / table map 收紧为闭合声明索引：value type 使用未导出 `valueKind`，record field 保存 label 与完整 type，table 保存 capacity、record type 与有序 primary key；map 只用于查找，不参与规范输出或摘要顺序。
33. table 构造现在拒绝空 / 重复 / 不存在 / `sensitive` primary key，并要求 key type 属于当前非可选 `bool` / `int` / `text` / `enum` 集合；12 份唯一 IR 的五种实际主键均为 `public text`。新增重新计算 definition domain ID 的完整最小 IR 正负例，分别覆盖四种可键类型、空 / 重复 enum、悬空 enum / record、字段缺失 / 重复、未知标签、错误整数范围和不受支持 `option`。
34. `checker.source` 重放为 606 个文件、168,514 bytes manifest 与 `sha256:9b6f7052b1bf186ed93a27063e6bab5ac94e4af4527bc9066b4084761cf12ac7`。以本机 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 运行全量 test / vet、源码身份门禁、脚本语法与 module 列表均通过；提交 `d36621ed94437b60f8ce289702e4bdf792d5d72b`、tree `a06c26841151e600549cc7a6c4a72022f19db3ab` 只作来源追溯，checker 工作树干净且仍无 remote。
35. 为锁定 18 个 expression op 增加独立返回类型推导：基础类型保留完整 Int 范围与名义 enum ID，table row / `lookup` 只在内部形成 `Record` / `Option<Record>`；`bound` 使用无名称环境，`field` 核对 record 与字段，Bool / 相等 / 比较 / 整数算术、`if` / `match_option`、table binder / lookup / count / sum 均失败关闭核对操作数、分支和结果类型。
36. filter / map 按单个前驱声明 record 建立 `[source_row]`，lookup join projection 建立 `[left_row, right_row]`，formula 顶层环境为空；filter predicate 与 formula 顶层必须为 Bool，map / join 的每个 projection expression 必须可独立推导。重算 definition domain ID 的合成负例覆盖缺失字段、record / bound operand、Bool / `eq` / `le` / 算术 / 分支、`match_option`、lookup key arity / type、量词 / 聚合 predicate、sum value 和 node table scope；25 个身份有效场景与 12 份唯一 IR 全部保持良构通过。
37. `checker.source` 重放为 608 个文件、168,903 bytes manifest 与 `sha256:eeedfdbaa9e87773e388ca24428ae6a94b943b3918ab2ede9d4ba39ef5052852`。以本机 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 运行全量 test / vet 与源码身份门禁均通过；提交 `a1d5b97f25044fb56418be70c9d8cc7275942508`、tree `35eee8d2cdd1f9e1aff3831b7bb5af93583b8159` 只作来源追溯，checker 工作树干净且仍无 remote。
38. node parser 不再丢弃 projection field name、lookup join pair、group key 与 aggregate 声明；独立关系检查按 canonical node ID 顺序核对 filter record / primary key / capacity，map / join projection 闭合与等型、主键直接保存 / 重命名和容量，以及 join pair 两侧字段存在与完整等型。`wrong-region-join` 的非键等型 pair 继续保持静态良构，“恰好一个右匹配”没有被错误提前假设或拒绝。
39. group 关系检查要求 keys 按顺序恰好等于输出 primary key、源字段 `public` 且可键、输出 key 等型、keys / aggregates 闭合覆盖 record、capacity 不超过输入；当前 `count` 固定 `Int[0, source_capacity]`，`sum` 只收口当前 profile 的非可选 Int 类型关系。重算全部受影响 definition domain ID 的合成正负例覆盖 4 类变换节点和 26 个失败关闭关系，25 个身份有效场景 / 12 份唯一 IR 全部保持通过。
40. `checker.source` 重放为 610 个文件、169,280 bytes manifest 与 `sha256:66859a2587068f64f8b4c94040deaccdc3439ee5507efce3326c28b8ae51845a`。以本机 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 运行全量 test / vet 与源码身份门禁均通过；提交 `777df21ef23fd7586a2bf80183a01a7a2c6da950`、tree `ed28e141b974e7dc7da44e2160f5819977ad93b6` 只作来源追溯，checker 工作树干净且仍无 remote。

本日没有安装任何工具或修改本机 Go 环境；先在隔离临时前缀执行已验收的 Go `go1.26.7` 和 checker parser 局部测试并删除临时工具链，后续 checker 实现切片只使用本机默认 Go `go1.26.3` 与隔离临时 cache。没有执行 solver、Node、生产编译器或正式模型调用，没有构建发布 binary、生成 `checker.artifact` 或独立 checker result、创建 remote、push、发布或产生六平台运行证据。

本次文档收口前已复核双仓今日已提交历史与当前未提交差异：主仓库的契约 / payload 物化和 checker 状态提交，与 checker 的仓库边界、严格 request / bundle、锁定语料、`checker.source`、Axiom IR / Evidence 分层检查及 concrete input / `Pre` 实现逐项一致。checker `README.md` 与相关实现说明已覆盖实际边界；主仓库规范、机器契约 README 与 ADR 0008 没有因本切片产生公共语义或格式变更，保持不动。

## 今日进展（2026-08-25）

1. 日初核对双仓状态：主仓 `dev` 与 `origin/dev` 同步且工作树干净；checker `dev` 无 remote，保留上一日完整 concrete input / `Pre` 未提交切片，没有通过 reset、checkout 或清理命令制造基线。
2. 复核上一切片实现、说明、source identity 与全量门禁后，将 concrete input / `WF` / `Pre` 作为独立主题提交为 `9aa1646d4d01ca3d995773f25862e7c1a11acd28`，使 IR 执行从可追溯干净基线开始。
3. 重新按 Evidence obligation expectation 与 counterexample kind 盘点 20 个 `failed` 条目，纠正原计划“16 个非 input-conformance 反例”的错误口径：实际为 9 个 input-conformance 条目、8 个 `prove + failed` proof target 和 3 个 host / output mismatch 条目。前两类进入本日既有与新增检查路径，后三项按停止线留给 output comparison。
4. 将原 input-only evaluator 收口为共享 concrete evaluator，继续使用同一套类型化 value / record / table、De Bruijn bound 环境、相等、名义 enum、数学整数与范围语义；没有建立第二套 runtime schema 或放宽 `WF` / `Pre`。
5. 新增确定性 IR DAG 执行：覆盖锁定 5 类 node、18 类 expression 与 count / sum 两类 aggregate，按稳定 DFS 拓扑顺序处理与序列化顺序无关的 DAG，并在每个中间 table 后重新检查声明 record、字段、类型 / 范围、容量、主键唯一性和 canonical order。
6. 执行路径对算术溢出、join 零匹配 / 多匹配、输入 port / 前驱 / projection 漂移、未闭合字段、步骤与逻辑内存超限失败关闭；只有明确的语义执行失败可参与反例目标判定，资源或内部错误不能伪装成目标违反。
7. 新增六类 target replay：contract-guarantee、noninterference、key-cardinality、field-origin、row-coverage 与 group-conservation；严格绑定 Evidence trace、observed obligation、required field / key 与 IR obligation。8 个实际 proof target 全部独立重放为违反，paired-input 非干扰按 IR 标签和受保护公开输出同时核对公开等价。
8. 正确候选回归在全部锁定 `checked` host input 上执行，不以 golden output 自证；负例覆盖序列化节点倒序、绑定漂移、字段遗漏、group sum 源字段漂移、算术越界、目标不再违反、trace / observed / public input 漂移及两类资源耗尽，既有 24 个完整场景和前置拒绝顺位保持不变。
9. 新增 checker target replay 专题说明并同步 README、concrete input 与 counterexample world 边界；host / golden comparison、minimality、proof / attestation、conclusion / result 继续明确在本切片之外。
10. `checker.source` 重放为 643 个文件、175,710 bytes manifest 与 `sha256:155469e8e9c915cfc4cbb22a1bce6daadd431c86810b9dc44b74289d0c025be5`。以本机 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 运行全量 `go test -count=1 ./...`、`go vet ./...`、源码身份门禁、脚本语法、`go list -m all`、`gofmt -l .` 与差异卫生检查，全部通过且 module 集合只有 checker 自身；这不是精确 `go1.26.7` 或六平台证据。
11. IR execution / target replay 提交为 `1f86de4d67d1c079d5fc20f77637caa9ef2299a4`、tree 为 `d0ff69b0b817b2e010b0ab284f496417d1583227`；checker 工作树干净且仍无 remote。Git 身份只作来源追溯，不替代 `checker.source` 或协议摘要。
12. 本日没有安装或升级依赖，没有执行 solver、Node、生产编译器或正式模型调用，没有构建发布 binary、生成 `checker.artifact` / 独立 result、创建 remote、push、发布或部署，也没有产生新的精确 `go1.26.7` 或六平台运行证据。
13. 从 24 个前置完整场景盘点 output 动态闭包：锁定 output data envelope 统一为 `role = golden-output`，但 Evidence execution 另以 `host-output`、`actual-output` 与 `golden-output` 区分语义身份；两层 role 不能互相替代或猜测。
14. 将 benchmark-data decoder 收口为同一方向化入口：input / output 共用严格 JSON、benchmark ID、Bool / Int / Text / 名义 Enum、record 与 table 解码，仅按所选 IR interface 集和允许 envelope role 分流；没有复制第二套 output codec。
15. 新增完整 output world 检查与无损相等关系：要求恰好完整 output interface，继续检查字段闭包、类型 / 整数范围、容量、主键唯一性和 canonical order；比较保留 interface、record type、field、value kind、enum identity 与显式数组顺序。
16. 新增 `VerifyConcreteOutputs`：从 output obligation 和 execution 图重建决定集合，追溯 actual 的 completed host producer，对每个相关 `host-input` 运行 checker 自身有限 IR，再分别核对 semantic、host / actual 与 golden；生产 target module 只核对边界，不执行。
17. 四题正确候选形成 8 条 host execution 和 7 条 checked comparison，全部满足 semantic = host = golden；`CHK-CONCRETE-01` 的故障 host execution 满足 semantic = golden ≠ actual。内容去重后共读取 8 个 output artifact，unknown output obligation 保持未决定，没有被升级。
18. `CHK-CONCRETE-01` 的一条真实不等 comparison 分别重放 3 个 failed entry：逐项核对 host / output subject、两步 trace、observed actual / expected、唯一 input-world witness 与 host producer，不能用 comparator `completed`、Evidence `failed` 或 raw bytes 不同自证。
19. 负例覆盖 output artifact format / envelope role、execute / compare I/O、checked host 输出对调、checked golden 替换、failed actual / expected 交换、trace / subject / witness 漂移、checked / failed 混用、raw digest、逻辑内存与 semantic step；关键 output 集成连续 20 次稳定通过，既有早期拒绝顺位保持不变。
20. 新增 checker concrete output comparison 专题说明，并同步 README、state / support、concrete input 与 target replay 的方法停止线；minimality、proof / attestation、conclusion / result 仍明确在本切片之外。
21. `checker.source` 重放为 646 个文件、176,310 bytes manifest 与 `sha256:d8496c37c6dacd282749b583ee99d2ec992c240700dee3374a2ad3901f531448`。以本机 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 运行全量 `go test -count=1 ./...`、`go vet ./...`、20 次关键重放、源码身份门禁、脚本语法、`go list -m all`、`gofmt -l .` 与差异卫生检查，全部通过且 module 集合只有 checker 自身；这不是精确 `go1.26.7` 或六平台证据。
22. concrete output comparison 提交为 `99765b564bcf3f3d4548bac68f739b389d149840`、tree 为 `6339d65eb9f39c82b4d9512b27830d9da5cd6d24`；checker 工作树干净且仍无 remote。Git 身份只作来源追溯，不替代 `checker.source` 或协议摘要。
23. 盘点 24 个前置完整场景的全部 proof support：共 213 个 producer `proved` claim，其中 148 个为 `kernel-replay`、65 个为 `backend-attestation`；20 个场景存在完成的 prove execution，四个 backend-timeout 场景没有 `proved`，既有 timeout / failed / checked / trusted 状态没有被新路径改写。
24. Evidence parser 不再丢弃 prover tool 的 artifact / name / immutable version 和 trust 的精确 scope；`InspectProofSupports` 按 obligation ID 稳定排序，重新核对每项 proved→execution→tool / role 关系，并通过 `bundle.Verified.ReadArtifact` 重开共享 obligation-set、query、response 与 tool artifact，逐一复算 raw SHA-256。
25. 生产 `axiom-obligation-set` 仍不参与独立 obligation 生成；只有在 completeness 已通过后，才核对其 IR raw / document 双摘要、semantics、profile、全部 definition / domain ID、排序和基数与已检查集合完全一致。query inspector 只接受闭合 QF_UFLIA ASCII envelope、平衡括号、有限顶层 command 和唯一 `check-sat`；cvc5 response 只接受单个精确 status frame，不解释 SMT term 或把 status 当证明。
26. checker 源码内显式能力集合保持 `KernelRuleProfiles = []`、`CertificateProfiles = []`。148 个 `kernel-replay` 全部以 `kernel-replay-material-unavailable` 形成缺失材料，wrong 场景共享 response 为 `sat` 时也不会因 producer tag 而升级；65 个 backend attestation 只确认 query / response / execution / tool / trust 闭包并保留 backend trust，独立证明计数恒为 0。
27. proof-support 子策略分类与既有场景一致：`attestation-allowed` 下 53 个 claim 可作为 attestation-only 继续并保留 trust；`CHK-PROOF-01` 的 12 个 attestation 在 `certificate-required` 下明确缺少材料。加上 148 个无可重放材料的 kernel claim，共 160 个 `MissingProofMaterial`；审计方法返回成功不等于所有 claim 已证明。
28. 新增 obligation-set format / IR subject、query logic / 重复 status command、response status、attestation trust scope、proved execution、raw digest、缺失 reader、逻辑内存与 semantic step 漂移负例；另以 wrong 场景锁定 `sat + kernel-replay` 失败关闭，并连续 100 次确认 finding / artifact / trust 的确定性顺序。24 个场景总回归精确锁定 213 / 65 / 148 / 53 / 160 口径。
29. 新增 checker proof support 专题说明并同步 README；`checker.source` 重放为 649 个文件、176,902 bytes manifest 与 `sha256:884a786f5be9533bba28e456f2b9173b3235dc6ff5d70282fe7814e65fe7dbb1`。以本机 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 运行全量 `go test ./...`、`go vet ./...`、源码身份门禁与差异卫生检查全部通过；提交为 `068ae7e956987c490ee95d832e6a2e9e09938b81`、tree 为 `230b73eb9501c1b1d235c11beb8ce827384c43a9`，checker 工作树干净且仍无 remote。本切片没有运行 cvc5、Node 或生产 compiler，没有生成 checker binary、`checker.artifact`、独立 result 或六平台证据。
30. 日终逐项复核本日已有开发提交：checker 的 concrete input / `Pre`、有限 IR target replay、concrete output comparison 与 proof support 四笔实现提交，以及主仓对应三笔状态提交，代码、测试、专题说明与当前阶段口径一致；没有发现需要修改公共 Axiom IR、Axiom Evidence、Independent Check Contract、ADR 或机器契约的语义 / 格式漂移。
31. checker 早期专题说明仍把后续调用链停在 concrete output，且 proof support 负例清单漏记代码已覆盖的 `proved` execution 绑定漂移；现已同步 Evidence 结构、obligation completeness、state / support、concrete output 与 proof support 五份说明，重放 `checker.source` 为 649 个文件、176,903 bytes manifest 与 `sha256:56394aa8ec205b2980d9866af85fd449cc7a6949cb34715829f612b2704f4811`。文档提交为 `ab9f4ed9e63c87bc57f012ff32a047b6710b590d`、tree 为 `f700b086af9c7d2dff898c2ec1abbcd19ab745c2`；Git 身份只作来源追溯。
32. 日终以本机 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 复跑 checker 全量 `go test -count=1 ./...`、`go vet ./...`、源码身份门禁与差异卫生检查，全部通过；checker `dev` 工作树干净且仍无 remote。没有安装或升级依赖，没有执行 solver、Node、生产编译器或正式模型调用，没有构建 binary、生成 `checker.artifact` / 独立 result、push、发布、部署或形成新的精确 `go1.26.7` / 六平台证据。

## 今日进展（2026-08-26）

1. 日初核对双仓状态：主仓 `dev` 与 `origin/dev` 同步且工作树干净；checker `dev` 工作树干净且仍无 remote，没有通过 reset、checkout 或清理命令制造基线。
2. Evidence parser 现在保留 producer conclusion kind，并将每个 digest ref 独立解析为 obligation / execution 域；producer conclusion 仍只用于最后比较，不参与状态真值、优先级或决定性 ref 形成。
3. 新增 `Document.VerifyConclusion` 与显式 working-memory / semantic-step 预算；checker 按结构拒绝、输入拒绝、实现不一致、其他违反、未知 / 未完成、满足的规范顺位遍历已检查 obligation state，并只追踪当前 result 必需的 execution，不把保留的历史失败 attempt 误报为当前阻断。
4. 决定性 refs 按 `(kind, value)` 稳定形成：input rejection、implementation inconsistency 与其他 violation 只保留各自 failed obligation，inconclusive 保留全部阻断 obligation 或 otherwise-complete 状态的未完成 execution，satisfied refs 为空。kind / ref 基数 / 域 / 值 / 顺序任一漂移使用既有 `conclusion-mismatch` 失败关闭。
5. 24 个前置完整场景全部独立重算并精确匹配 producer：7 个 `satisfied`、4 个 `input_rejected`、8 个 `violated`、4 个 `inconclusive`、1 个 `implementation_inconsistent`。`structure_rejected` 规则在内部优先级闭合，但没有用无法形成有效 IR Document 的前置拒绝伪造锁定正例。
6. 负例覆盖 producer 原始 Evidence kind 篡改、ref 缺失 / 多余、错类、错值、顺序、优先级、必需 execution 完成状态和两类资源边界；failure 与 unknown 并存仍稳定选择 failure，相同输入连续 100 次产生相同 kind / refs。
7. production `satisfied` 只核对 Evidence 对自身五态的忠实聚合，不读取或覆盖 `ProofSupportCheck`；总回归继续精确保持 213 个 producer `proved`、0 个独立 proof、65 个 attestation、148 个 unsupported kernel claim、53 个 policy-satisfied attestation 与 160 个 missing proof material。
8. 新增 production conclusion 专题说明，并同步 README、Evidence 结构、state / support、concrete output 与 proof support 调用链；没有修改 Axiom Evidence、Independent Check Contract、ADR 或机器 fixture 公共格式。
9. `checker.source` 重放为 652 个文件、177,483 bytes manifest 与 `sha256:6f52f4756b163868f54e6858f777be6894a7179e374ce9cbaf73592b247dcbf8`。manifest 与上一身份相比只改变本切片的 13 个预期源码 / 测试 / 文档路径。
10. 以本机 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 运行全量 `go test -count=1 ./...`、`go vet ./...`、源码身份门禁、脚本语法、`go list -m all`、`gofmt -l .` 与差异卫生检查，全部通过且 module 集合只有 checker 自身；这不是精确 `go1.26.7` 或六平台证据。
11. 本切片没有安装或升级依赖，没有执行 cvc5、Node、生产编译器或正式模型调用，没有构建 binary、生成 `checker.artifact` / 独立 result、创建 remote、push、发布或部署。conclusion 重算实现提交为 `1e8075ed2e11866c1222e4094e5f459be9af0693`、tree 为 `f4370ff3501af87e804e8ea5bfea3ed6c0f2c215`；checker `dev` 工作树干净且仍无 remote。Git 身份继续不能替代 `checker.source` 或协议摘要。
12. 新增 `internal/checkresult` 内存结果层，十类 check 只接受 Independent Check Contract v0.1 的闭合 kind / outcome / code / ref，definition 规范排序后使用 `axiom-independent-check-v0.1:check` 域形成 ID；严格 Evidence 拒绝 fixture 的黄金 check ID 精确匹配，未生成完整 companion 编码。
13. request parser 现在独立形成 raw content digest 之外的 `axiom-independent-check-v0.1:request` document domain digest；结果身份边界同时绑定 Evidence / request 双摘要、checker source、实际调用 toolchain / 版本与 canonicalization、checker-core、cryptographic-primitive、rule-interpreter 四类必需 TCB source。source identity 不能替代尚未构建的 `checker.artifact`。
14. `bundle.Verify` 对所有现存 blob 继续核对普通文件、长度、资源和 raw SHA-256；manifest 已列出但缺失的 blob 改为排序唯一的 `MissingArtifacts`，不再与 digest / symlink / 路径等确定矛盾混为拒绝。`ReadArtifact` 对缺失项仍失败关闭，不使用网络、cache 或邻接目录补齐。
15. Evidence 的全部 trust definition 由 `TrustInventory` 按 ID 保守保留；本切片不因独立 parser / replay 自动删除 producer trust。未被 request 明确允许的类别使 state / result 保持 incomplete，允许且非空的 remaining trust 使完整结果最多为 accepted-with-trust。
16. `EvaluateVerifiedBundle` 直接调用现有 strict parse、identity、subject、obligation completeness、state / support、counterexample world / target、concrete input / output、proof support 与 conclusion 路径形成真实 checks；expected result 不进入实现入口，只在测试末端比较版本化 outcome、remaining trust 与 missing artifact。
17. 四态聚合唯一顺位实现并负例锁定：任一 rejected check 优先，其次 incomplete / missing / 不允许 trust，再次允许且非空 remaining trust，最后才是全部十类检查完成且 trust 为空的 accepted；非 accepted 结果保留决定它的 check ID refs，拒绝不会吞掉并存的 missing / trust 清单。
18. 25 个实际进入结果层且具有 expected companion 的场景形成 22 个 accepted-with-trust、2 个 incomplete 与 1 个 rejected；正确报告 violated、input_rejected 和 implementation_inconsistent 的 Evidence 都可独立接受。`CHK-PROOF-01` 的 12 项 attestation 在 certificate-required 下保持 incomplete，`CHK-PROOF-02` 在 attestation-allowed 下保留 trust，二者独立证明数均为 0。
19. 新增结果层专题说明并同步 checker README、Evidence 结构、proof support 与 conclusion 调用链；没有修改 Axiom Evidence、Independent Check Contract、ADR 或机器 fixture 公共格式，也没有生成 canonical companion document。
20. `checker.source` 重放为 658 个文件、178,622 bytes manifest 与 `sha256:5db26879872044fc68a42a0254eb43fe45b28b6dc61c063b82ca749cdbba6b1f`。以本机 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 运行全量 `go test -count=1 ./...`、`go vet ./...`、源码身份门禁、脚本语法、`go list -m all`、`gofmt -l .` 与差异卫生检查，全部通过且 module 集合只有 checker 自身；这不是精确 `go1.26.7` 或六平台证据。
21. 本结果切片没有安装或升级依赖，没有执行 cvc5、Node、生产编译器或正式模型调用，没有构建 binary、生成 `checker.artifact` / canonical 独立 result、创建 remote、push、发布或部署。双仓当前保留本任务未提交差异；Git 身份继续不能替代 `checker.source` 或协议摘要。
22. 按仓库边界提交上一切片：checker `f958acf` 只包含四态结果实现、测试、专题说明与源码身份，主仓 `6952c62` 只同步当前阶段和下一顺位；提交后两仓均从干净工作树继续开发，没有 amend、rebase、push 或改写历史。
23. 新增 Independent Check Contract v0.1 canonical companion encoder 与严格 parser。encoder 只编码已经形成的内存 `Result`，按 check ID、code、ref、missing、remaining trust、result refs 与 TCB `(category, artifact)` 唯一排序，并重算 `axiom-independent-check-v0.1:result` document domain digest；parser 独立重算 check ID、四态顺位、ref 存在性、必需 check 集合和 document availability，不读取 expected result 决定实际字节。
24. companion 形成必须显式传入当前平台 checker binary 与 runtime TCB artifact，并与 source-level category / version 边界一一匹配；`checker.source`、Git commit / tree、toolchain 或 TCB source 均不能替代 binary。非空 artifact、精确 `go1.26.7`、非 `latest` 实现版本和四类必需 TCB 是正式编码前置条件；当前没有实际 binary，因此真实开发结果仍停在内存层。
25. 严格 Evidence 拒绝黄金 fixture 精确重放为 1,685 bytes、raw `sha256:f61b746a3e85f8edf6e8e5d5bbbba2550081565b6a2d22bc73003dbe136b3089` 和 result document `sha256:24e81c66e17150c70c1b2d2eac50b47f16fc20c6a094111be3410546c7b6e608`；27 份指定态 companion 全部由 parser 接受，25 个 actual result 场景全部完成 encode → strict parse round-trip。负例覆盖 binary 缺失、source / binary / TCB 混用、check ID、result kind / ref、数组顺序、document availability、runtime identity 漂移和 100 次确定性重复。
26. 新增 `axiom-checker-invocation-failure` `0.1` encoder / parser；它只绑定 canonical request 的 raw / document 双身份与 `result: not-produced`，不含四态、checks、trust 或 TCB，也不能被 result parser 接受。`CHK-PROCESS-01` 生成字节与锁定 `expected-process-failure.jcs` 精确一致；launcher 的 kill / crash / truncation 观察仍须由外层保存，不能由 envelope 自证。
27. 当前尚未建立完整 invocation orchestrator：`chk-digest-01` 的前置身份拒绝与 `chk-resource-01` 的内部资源不足仍由既有入口返回错误；parser、artifact、semantic-step 与 logical-memory 预算也仍是分切片局部上限，尚未累计到一次 invocation，内部 wall-clock 中断未实现。本轮没有以无限预算、expected companion 或 fallback 冒充该边界已经完成。
28. `checker.source` 日终重放为 664 个文件、179,786 bytes manifest 与 `sha256:79728ca596a49f5b88eec4e67ed71a7565e9174d98691a401f5eb9a6cc929cf7`。以本机 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 运行全量 `go test -count=1 ./...`、`go vet ./...`、源码身份门禁、脚本语法、`go list -m all`、`gofmt -l .` 与差异卫生检查，全部通过且 module 集合只有 checker 自身；主仓仓库级门禁同时通过 867 个文件。这不是精确 `go1.26.7` 或六平台证据。
29. canonical companion / invocation failure 实现提交为 `c1a953fae815196dac694e37994879e4680682de`、tree 为 `fad4db80780e85e33b165aa05d5f3d8b7aeda7d7`；checker 专题说明、调用链修正与源码身份提交为 `35727a2e2681cbea84ef820d201f0d9f746c9cf1`、tree 为 `9a002f47dac7460443b6cb2431695e050a6b0670`。Git 身份只作来源追溯，不能替代协议 source / artifact 摘要。
30. 日终复核今日 checker 的 production conclusion、四态结果聚合、canonical companion 三笔实现提交和一笔文档提交，以及主仓本次收口前的两笔状态提交。代码、测试、README、结果聚合、conclusion、proof support 与新增 companion 专题的调用链已经一致；没有发现需要修改 Axiom IR、Axiom Evidence、Independent Check Contract、ADR 或机器 fixture 公共语义 / 格式的漂移。
31. 本轮没有安装或升级依赖，没有执行 cvc5、Node、生产编译器或正式模型调用，没有构建或验收 binary、生成真实 `checker.artifact`、创建 remote、push、发布或部署。canonical 测试使用的 binary / TCB digest 仅为锁定合成身份；checker `dev` 工作树干净且仍无 remote，主仓最终提交范围只包含本次日终状态文档。

## 今日进展（2026-08-28）

1. 日初核对双仓状态：主仓 `dev` 与 `origin/dev` 同步且工作树干净；checker `dev` 工作树干净、仍无 upstream / remote，没有通过 reset、checkout 或清理命令制造基线。
2. 新增一次调用唯一的 `resourcebudget.Ledger`；request 解析期间先在实现硬上限内累计但延迟暴露 request 级资源不足，待真实 request / Evidence / IR 与 checker identity 足以形成规范结果后统一 `Activate`，首个 resource finding 保持 sticky。
3. bundle invocation 入口先按 manifest 预计算唯一 artifact 声明总字节，再逐个核对现存 blob 并累计 64 KiB digest block；重复打开 artifact 不重复增加 bundle bytes，但真实重新散列继续增加 semantic step。确定 digest finding 被保留到身份可绑定，不由后续 resource exhaustion 隐藏。
4. strict JSON parser 将 token、object member / array element 和最大打开容器深度接入同一累计计数器；obligation completeness、state / support、counterexample world / target、finite IR expression / row / node、concrete input / output、proof support 与 conclusion 的既有确定性 step / logical memory 预算也接入同一 ledger，并按稳定 owner 释放已丢弃对象。
5. 十类 check 前后、每跨过 1,024 semantic steps 与 result 编码前新增内部 monotonic wall-clock checkpoint。预算耗尽以 `isolation-report` / `tcb-incomplete` 形成 canonical `incomplete`；若已存在真实 rejected check，四态聚合继续让拒绝优先，同时保留资源不足事实。
6. 新增 `InvokeBundle` / `RecordInvocationFailure` 闭合 union：正常 bundle 走十类完整检查并保留累计快照，`CHK-DIGEST-01` 形成真实 `rejected`，`CHK-RESOURCE-01` 形成真实 `incomplete`，`CHK-PROCESS-01` 只形成绑定 request 的 `not-produced` failure。实际形成路径不读取 expected result 决定 outcome 或字节。
7. 负例覆盖 digest 与 semantic budget 同时触发、request 不可读、request 非规范、checker source 缺失、内部 wall-clock 耗尽、编码前资源耗尽和 companion 截断；无可绑定 request / checker identity 时不产生四态，外层 kill / crash / truncation 也不能伪装内部 incomplete。
8. 新增 invocation / 累计资源专题说明并同步 checker README、canonical companion、内存聚合和各局部 IR / Evidence 专题的调用链；没有修改 Axiom IR、Axiom Evidence、Independent Check Contract、Execution Profile Contract、ADR 或机器 fixture 公共格式。
9. `checker.source` 重放为 669 个文件、180,745 bytes manifest 与 `sha256:3582d0cddcf782c83b0657b40137db6ee7c66d7bc2e81ba32342e69639ba1160`。以本机 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 运行全量 `go test -count=1 ./...`、`go vet ./...`、源码身份、`gofmt` 与差异卫生门禁全部通过；这不是精确 `go1.26.7` 或六平台证据。
10. checker 实现、测试、专题说明与源码身份按仓库边界提交为 `0a8dd5b0e56efc462857a1434f1614b18a9d0fe3`，tree 为 `c89d6a219a9deba6118f973de4c6fa1677dc319d`；Git identity 只作来源追溯，不能替代 `checker.source` 或 binary artifact identity。本切片没有安装或升级依赖，没有执行 cvc5、Node、生产编译器或正式模型调用，没有构建或验收 binary、生成真实 `checker.artifact`、创建 remote、push、发布或部署。
11. 新增唯一产品命令 `cmd/radishaxiom-independent-checker-go`，只接受精确 `check` 与 `--bundle-root=<canonical-realpath>` 两个 token；CLI 拒绝别名、分离 option value、额外参数、相对 / 冗余 / symlink / 缺失路径及非空 stdin，并只把通过的只读 bundle 交给既有 `InvokeBundle`，没有建立第二套 parser、聚合、资源或 companion 路径。
12. CLI 运行时身份在 bundle I/O 前失败关闭：受控构建必须 linker 注入合法 `checker.source` 与精确非 `latest` 实现版本；toolchain 只能取进程 `runtime.Version()` 且必须为 `go1.26.7`；executable 必须是 canonical realpath 下的普通文件，并在读取前后核对文件身份、mode、length 后流式复算 SHA-256。该摘要同时绑定 `checker.artifact` 与当前单体四类 runtime TCB artifact，不能由 source、Git identity、参数、环境或 PATH 替代。
13. CLI 成功只向 stdout 写一个无尾随换行的 canonical result，stderr 为空；参数 / stdin 错误与身份 / bundle / 输出错误分别非零退出，诊断限定为一条不超过 65,536 bytes 的 UTF-8 stderr。单元与真实子进程测试覆盖 normal、`CHK-DIGEST-01`、`CHK-RESOURCE-01`、非法参数、symlink、非空 stdin、bundle / identity failure、截断输出、8 次字节确定性及 OS 级 stdout / stderr / exit 分离；测试的 `0.1-test` toolchain / executable identity 明确为合成边界。
14. CLI 专题说明、README 与既有 invocation / companion 调用链已同步；`checker.source` 最终重放为 675 个文件、181,868 bytes manifest 与 `sha256:f05a697f67841380767c2ea405919e3dafe85dd5d747969516c965361cee65f9`。以本机 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 运行全量 `go test -count=1 ./...`、`go vet ./...`、源码身份、脚本语法、module 闭包、`gofmt` 与 checker 差异卫生门禁全部通过；module 仍只有 checker 自身。
15. 本机开发命令分别验证未注入 source 时因身份缺失退出、注入 source / `0.1-dev` 后仍因实际 `go1.26.3` 不等于 `go1.26.7` 而在 bundle I/O 前退出；临时 binary 已删除。一次未带 `GOTOOLCHAIN=local` 的只读 `go version -m` 检查尝试解析精确 toolchain，但因沙箱禁止写入而未下载或安装任何内容，随即改用本机 toolchain 完成检查。本切片没有生成或验收正式 `checker.artifact`、receipt、remote、push、Release、发布或部署。
16. checker CLI 实现、测试、专题说明与源码身份按仓库边界提交为 `caa546e3020832717f4d10c9b2d6108d3984fd55`，tree 为 `1e2eb55e030e30ff37490bc861847c919d167036`；提交后 checker `dev` 工作树干净且仍无 remote。Git identity 只作来源追溯，不能替代 `checker.source`、受控构建 provenance 或 binary artifact identity。
17. 新增唯一 `radishaxiom-checker-build build` 受控入口和版本化 build provenance：闭合 source / toolchain archive / 空 output / version 四项 canonical 输入，使用 Go 标准库复算归档、安全解包、核对 inventory 和实际 toolchain；在两个隔离状态根中构建并要求 binary 原始字节一致，任何 source identity、archive、toolchain、mode、输出或可重复性漂移都失败且不物化候选。
18. 新增小于 builder 的 `radishaxiom-checker-accept accept`：不调用编译器，独立核对闭合 build root、Mach-O、Go build info、module / settings、artifact / provenance 原始身份，并在空环境、隔离 cwd、6 秒 deadline、1 MiB stdout / 64 KiB stderr 上限下运行 `ax-b01-correct`、`chk-digest-01`、`chk-resource-01`；使用标准库 JSON canonical round-trip 小路径检查 binary / source / toolchain / version / TCB 与三种结果，不复用生产 strict parser 自证。
19. payload 构建切片的 `checker.source` 重放为 682 个文件、183,203 bytes manifest 与 `sha256:3b809ba6f062dbc1c543b64d9eb01f1fe46bdadf29f2d441b6d1c068200d90b4`。以本机 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 运行全量 `go test ./...`、`go vet ./...`、源码身份、module 闭包、脚本语法、`gofmt` 与差异卫生门禁全部通过；module 仍只有 checker 自身。本机 Go 只启动 orchestrator，不充当候选 binary 的编译器。
20. 经当前任务单独授权，将主仓已局部接受且未持久保存的官方 `go1.26.7.darwin-arm64.tar.gz` 下载到临时目录；网络中断后只保留闭合区间并以小区间续取，最终重新组装的归档精确为 64,772,572 bytes 与 `sha256:020a1e8224811be75163e920bc77e0926a1390a6aeea19bdcf23f74b9d749f6d`。没有安装或替换系统 Go，没有修改 PATH / GOROOT，也没有把未核验的分段交给 builder。
21. 精确归档内 `go1.26.7` 对同一 source / `0.1-dev` / darwin-arm64-v8.0 完成两次隔离构建并得到完全相同的 4,689,378-byte binary，`checker.artifact = sha256:ef66c4d9098d058796e4f9d7e0f7713e73fc954e94c97a4001abd28acdbd99dc`；1,389-byte canonical provenance 为 `sha256:7916666ae8f2883f5787a58307253416a653f8aca69e1cd1babc8f22639a38e0`。
22. 独立 payload acceptance 重新检查候选并决定 `accepted-for-controlled-runtime-registration`；1,580-byte acceptance 为 `sha256:fb148d749622511ab6d13bcc2c35db10076a4cc9286e95d08443d3822ca1742a`。三条实际 canonical result 分别为：`ax-b01-correct` / `accepted-with-trust` / 7,699 bytes / `sha256:0f519d63ce293548c2988915ee0e9dd269ad3632e9c4f7a06c0ff3cb6332a97b`，`chk-digest-01` / `rejected` / 2,254 bytes / `sha256:97695d7a83bf2f3b46823037d2e7f2a44cde0754cd9d5727c39d9d7da1b72875`，`chk-resource-01` / `incomplete` / 2,935 bytes / `sha256:4b70570fcdf47be6d4cb329eb4da3de3739fb1557316a8a2630625f000879251`。
23. 本地 acceptance 的 accepted scope 仅为 artifact byte reproducibility、build metadata、runtime self-identity 与三条 scenario behavior；明确排除 cross-platform equivalence、installation、launcher hard isolation、distribution legal compliance、publication 与 release signing。临时 archive、区间、cache、binary、provenance 和 acceptance 在记录精确身份后删除；本切片不修改公共 Evidence / Independent Check 格式，不登记 registry、不安装 payload、不创建 remote / push / Release / 发布 / 部署，也不外推六平台结论。
24. checker 受控构建、独立 acceptance、测试、说明与最终 source identity 按仓库边界提交为 `ea4f1bb2e6f4cb97ad8dbc0b15016f15586a62e2`，tree 为 `6a99414a7721d6650138ffe8819b9efb58e76eaf`；提交后 checker `dev` 工作树干净且仍无 remote。Git identity 只作来源追溯，不能替代 `checker.source`、binary artifact、provenance 或 acceptance 摘要。
25. 日终逐笔复核今日 checker 的 invocation 累计资源、产品 CLI、受控构建 / acceptance 三笔实现提交 `0a8dd5b0e56efc462857a1434f1614b18a9d0fe3`、`caa546e3020832717f4d10c9b2d6108d3984fd55`、`ea4f1bb2e6f4cb97ad8dbc0b15016f15586a62e2`，以及主仓对应的 `30b525aa5d5e12317f4c8d371128e8826ef5f7f8`、`68ca51ee7dbdd0e229e00bc718dfee56a487c9c5`、`022cc008e29714e05a669fa13b42556a8b86cf6f` 三笔状态提交。代码、测试与主专题边界一致；发现 proof support、result aggregation、canonical companion、invocation 和 CLI 五份 checker 专题仍有被后续实现超越的“尚无 CLI / binary”状态措辞，未发现 Axiom IR、Axiom Evidence、Independent Check Contract、ADR 或机器 fixture 公共格式漂移。
26. 五份 checker 专题只修正后续实现状态和调用链，不改公共字段、tag、排序、域摘要或实现代码；文档属于闭合 source 输入，因此重新形成 682 个文件、183,203 bytes manifest 与当前 `checker.source = sha256:5edf65123fdeda4b2cf37476f46ab02b4bce1875e41a2677f80ddf55cda1147b`。全量 `go test ./...`、`go vet ./...`、源码身份、module 闭包、脚本语法、`gofmt` 与差异卫生再次通过；文档 / source identity 提交为 `6b9532d95016f5bb62b53c730fbf3d95d46fd388`，tree 为 `3d0c76de3d300e7c5d4e2ea13cb53213c4208afd`。
27. checker 本地仓库当前已有 25 笔可审计提交，但只有 `dev` 分支、没有 `master`、remote、远程 CI 或独立发布流水线。它已达到准备远程仓库的工程阶段：继续长期只保留本地历史不利于独立仓库、独立检查和后续发布边界；但 owner、仓库名、可见性、默认分支、首推 refs、CI 和 Ruleset 都是外部治理状态，今晚没有创建 remote 或 push，必须在明日确认精确参数并获得单独授权。

## 今日进展（2026-08-29）

1. 日初核对双仓状态：主仓 `dev` 工作树干净且比 `origin/dev` 超前 4 个状态文档提交；checker `dev` 工作树干净、已有 25 笔本地提交，尚无 `master`、remote 或远程 CI，没有通过 reset、checkout 或清理命令制造基线。
2. 在 checker 内形成远程建仓决策单并补齐最小治理资产：`.editorconfig`、`.gitattributes`、贡献与安全入口、PR 模板、分支 / merge / Conventional Commits 规则、仓库卫生与 module 闭包脚本；没有复制主仓的 Rust、solver 或 Node 门禁。
3. 新增 `Checker Checks` workflow，精确固定官方 `actions/checkout` v6.0.2 commit `de0fac2e4500dabe0009e67214ff5f5447ce83dd` 与 `actions/setup-go` v7.0.0 commit `b7ad1dad31e06c5925ef5d2fc7ad053ef454303e`，只授予 `contents: read`；`Repository Governance` 与 `Checker Go Quality` 由稳定 `Candidate Quality` 聚合。CI 中取得的精确 Go 只用于测试 / 静态检查，不充当 payload builder 或 acceptance。
4. 治理资产进入闭合源码输入后，`checker.source` 重放为 691 个文件、184,791 bytes 与 `sha256:256dc873e83362ea3321a62a1ce00a30dc24e96cf79d6014adf2ef6208246b59`。以本机 Go `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 运行仓库治理、全量 `go test -count=1 ./...`、`go vet ./...`、源码身份、module 闭包、`gofmt` 与差异卫生，全部通过；这不是新 payload acceptance。
5. checker 治理提交为 `28ad1dbe8c61b9a34985630b4f26d9a885711b3e`，tree 为 `09e3dd134602deffa6527525102a512029b3d0b1`，是独立仓库第 26 笔提交；本地 `master` 与 `dev` 同指该提交。Git identity 只作来源追溯，不能替代 `checker.source` 或 artifact identity。
6. 经所有者明确确认公开可见性、owner、仓库名、描述、许可证呈现、remote、默认 / 集成分支、首推 refs 与 merge options，并授权外部动作后，创建公开仓库 `https://github.com/laugh0608/RadishAxiomChecker`，添加 HTTPS `origin`，首次推送 `master` 与 `dev`；两个远程 refs 经 GitHub API 与 `git ls-remote` 独立读取，均精确指向 `28ad1dbe8c61b9a34985630b4f26d9a885711b3e`。
7. GitHub 写后读取确认默认分支为 `master`，仓库公开且描述精确；merge commit / rebase merge / Issues 开启，squash merge / Wiki / Projects / Discussions 关闭。没有创建 Ruleset、tag、Release、部署或 payload。
8. GitHub Actions 的 `master` push run `33235447011` 与 `dev` push run `33235480072` 均在精确治理提交上完成并成功；两次 run 的 `Repository Governance`、`Checker Go Quality` 与 `Candidate Quality` 三个 job 均由 job 级读取确认为 `success`，稳定 required context 已实际产生。workflow success 是 CI 证据，不是形式证明或 runtime acceptance。
9. checker 最终工作树干净，`dev` / `master` 分别跟踪 `origin/dev` / `origin/master` 且无 ahead / behind。主仓本轮只同步正式状态源，不推送；历史 macOS arm64 payload 仍绑定旧 source，不得登记为当前 source artifact。
10. 主仓新增 Checker Runtime Payload Registration v0.1：两份域摘要记录分别保留旧 source 候选的精确 artifact / provenance / acceptance / 三场景历史身份及 `historical-ineligible` 结论，并把当前 `checker.source = sha256:256dc873e83362ea3321a62a1ce00a30dc24e96cf79d6014adf2ef6208246b59` 记为 `awaiting-controlled-build-and-acceptance`。契约显式记录三类原始字节的 retention / fetch / re-verification 条件，active runtime 为 0；6 个负例拒绝 source 重绑、retention 过度声明、历史候选冒充登记、安装范围扩张、孤立 artifact 摘要和未知 member。仓库门禁已接入离线生成复核，没有下载、构建、执行、安装或发布 checker。
11. checker 新增唯一 payload 候选归档命令：`pack` 将闭合 acceptance 目录中的 artifact、canonical provenance / acceptance 与新 retention manifest 按固定 member 顺序、mode、uid / gid、mtime 和无扩展 header 确定性封装为未压缩 USTAR；`verify` 严格重算成员摘要、canonical manifest 与完整 archive bytes，拒绝额外文件、错误模式、未知字段、非规范 JSON、member / header / 尾随字节漂移，并在写入前后重放 source 防止竞态。源码身份更新为 695 个文件、185,571 bytes 与 `sha256:675f8ff470a621124ede081bcf8330b73910ab34e20b7b174356d57423e6ee74`；本机 Go `go1.26.3` 的全量 test / vet、source identity、module 闭包、gofmt、治理和差异卫生通过，但不是精确 `go1.26.7` payload 证据。主仓 pending record 与两级 storage policy 同步：Actions artifact 仅作最多 90 天、按精确 artifact ID 和 provider 回读绑定的候选暂存，永远不能激活 runtime；durable active provider 仍未选择，GitHub immutable Release 也未获发布治理或外部授权。本切片没有下载 Go、构建 / 执行 binary、上传 artifact、运行远程 workflow、创建 tag / Release 或 active registration。
12. checker 新增隔离的 `Checker Payload Candidate` 手工 workflow：要求显式当前 source、精确 version 与上传确认，仅接受 `dev` / `master` ref；`macos-15` arm64 job 先复跑仓库门禁，再核对官方 Go archive 后执行两次隔离 build、独立 acceptance 与 deterministic pack，精确固定 `actions/upload-artifact` v7.0.1 以 `archive: false` 直传单一 USTAR。独立 Ubuntu job 用 artifact ID 和精确固定的 `actions/download-artifact` v8.0.1 原样回读，复算 archive / manifest 身份、运行严格 verifier，并核对 provider REST API 的 ID、name、size、digest、run、head SHA、created / expires；summary 只形成非 JCS 候选交接 JSON。治理脚本将 manual-only、arm64 runner、90 天、direct-file、按 ID 回读与四个 action commit pin 固化为静态门禁。源码身份更新为 696 个文件、185,774 bytes 与 `sha256:e2c4ae31b15162051735a76e44c2fc0a079117994caf3ef53d5710ead1199044`；workflow 实现提交为 `014f35cc268cb6cce6c35b840841696cff538f7b`，tree 为 `128ea8b12c49fe5319783930e74bb3f089233bb9`，checker 工作树干净且本地 `dev` 比 `origin/dev` 超前 2。主仓 pending record 和 storage policy 同步为 `workflow-committed-not-deployed-or-materialized`。本地 Go `go1.26.3` 的全量 test / vet、source identity、module 闭包、gofmt、YAML / bash syntax、治理与差异卫生通过，但不是精确 `go1.26.7` payload 或 GitHub provider 证据；一次未带 `GOTOOLCHAIN=local` 的 `go version` 检查尝试自动解析精确 toolchain，因沙箱在缓存目录写入前拒绝而未下载或安装，随后以显式本机 toolchain 和隔离 cache 重跑通过。本切片只形成两仓本地提交，没有 push、把 workflow 带入默认分支、运行远程 workflow、下载 Go archive、构建 / 执行 binary、上传 artifact、创建 tag / Release 或 active registration。
13. 经所有者对精确目标的单独授权，将 checker 本地 `dev` 的两笔提交快进推送到 `origin/dev`；写后读取确认远程 ref 精确为 `014f35cc268cb6cce6c35b840841696cff538f7b`。该 push 触发常规 `Checker Checks` run `33238577710`，其 `Repository Governance`、`Checker Go Quality` 与稳定聚合 `Candidate Quality` 三个 job 均在该 head SHA 上成功。候选 workflow 没有自动触发，仍未进入默认分支 `master`；主仓 pending record 与 storage policy 同步为 `workflow-on-dev-not-default-branch-or-materialized`，active runtime 保持 0。本步没有推送主仓、创建 PR、合并 `master`、运行候选 workflow、下载 Go archive、构建 / 执行 binary、上传 artifact、创建 tag / Release 或 active registration。
14. 经所有者分别授权创建 PR 与合并 / 回流，checker `dev -> master` PR #1 在 head `014f35cc268cb6cce6c35b840841696cff538f7b` 上完成常规 `Checker Checks` run `33239514736`，三个稳定 job 均成功；随后以 merge commit 合并为 `a81f5b4704efddd1d8c293f9e8e47c58149e65b7`，其双亲精确为旧 `master` `28ad1dbe8c61b9a34985630b4f26d9a885711b3e` 与已审查的 `dev` head。`master` push run `33239697298` 的 `Repository Governance`、`Checker Go Quality` 与 `Candidate Quality` 均成功；再将该 merge commit 仅以 fast-forward 回流到 `dev`，push run `33239918358` 的相同三个 job 均成功。写后读取确认远程 `master` / `dev` 与 checker 本地 `dev` 均精确指向 `a81f5b4704efddd1d8c293f9e8e47c58149e65b7`，工作树干净。主仓 pending record 与 storage policy 将实现身份 `014f35cc...` / tree `128ea8b...` 和默认分支部署 merge 身份分开记录，状态更新为 `workflow-on-default-branch-not-materialized`，active runtime 保持 0。候选 workflow 没有运行，没有下载 Go archive、构建 / 执行 binary、上传 artifact、创建 tag / Release、发布、部署或 active registration。
15. 本轮启动时主仓 `dev` 工作树干净、HEAD 为 `28c2b957eed04f1ad5f4a764b5ebaa8b2d449ce3` 且比 `origin/dev` 超前 10；checker 检出的 `dev` 工作树干净且为 `a81f5b4704efddd1d8c293f9e8e47c58149e65b7`，但本地未检出的 `master` 仍停在旧 `28ad1dbe8c61b9a34985630b4f26d9a885711b3e`。没有 reset、checkout 或快进本地 `master`；GitHub API 独立确认远程 `master` / `dev` 均为 `a81f5b4704efddd1d8c293f9e8e47c58149e65b7`，候选 workflow active 且历史运行数为 0。
16. 经所有者对本次唯一远程写入的明确授权，在精确 `master` 触发 `Checker Payload Candidate` run `33246312135` / attempt 1，输入当前 `checker.source`、`version = 0.1-dev` 与显式上传确认。run 与两个 job `99084274782` / `99084367932` 均为 `success`；source / 精确 `go1.26.7` / 仓库门禁、双次隔离原始字节一致构建、独立 acceptance、确定性 pack、单文件直传、provider digest 匹配，以及 Ubuntu 按 artifact ID 原样下载、严格 archive verifier 和 REST metadata 核对全部通过，没有重跑。
17. 当前 source 候选 binary 为 4,689,378 bytes / `sha256:02bdc09e3628a61364c5f5638245b9c5b745f20246ab34ff5d278503bb73a2e5`；canonical provenance 为 1,389 bytes / `sha256:74777711e628da1ae3862b488eedaca7741cb60d90f173021ce72d5f814b4812`，canonical acceptance 为 1,580 bytes / `sha256:afbfe9aef14573d5a4facfe5290962684027614099117273c953dab701a079a4`，决定为 `accepted-for-controlled-runtime-registration`。三条 acceptance 场景分别得到 `accepted-with-trust` / `sha256:9c83df7948060b050fe8743e4933ba9f9948a2d4ee1c2bf3cb50aa3c5687ba3a`、`rejected` / `sha256:56de23b703ee5ce62ca33aa42b1d969b40d81360aec3ae2a622f9d5a04b88909` 与 `incomplete` / `sha256:01dd88943b954fe79e893eb563f273bd35689be2f7565a1d8ac8a98e6e76bc2a`；1,295-byte retention manifest 为 `sha256:f357e32d2832b81ee4fb5f95822d4067a002d0964a25c966f339542fc5d2667c`，4,697,600-byte USTAR 与 provider direct-file digest 同为 `sha256:5e567fb39e2971db6b971f7139dfb54c4c7d6bbb58b8c9b90775c2c9cb6eb701`。
18. GitHub Actions artifact ID `9712952210` 的 provider name、size、digest、run、head SHA 与 workflow 输出一致，回读时 `expired = false`；provider 创建时间为 `2026-08-29T09:48:31Z`，到期时间为 `2026-11-27T09:47:39Z`。主仓 pending registration 已按实际输出更新为 `candidate-retained-temporarily`，三类规范字节可在该 USTAR 存续期间按精确 artifact ID 取得，active runtime 仍为 0；没有修改 Ruleset、创建 tag / Release、激活或安装 runtime、推送主仓，也没有删除远端 artifact。
19. 主仓 `python3 scripts/generate-checker-runtime-payloads.py --check` 通过 11 个生成文件，`git diff --check` 通过，`./scripts/check-repo.sh` 通过 880 个仓库文件。最终 provider 复核确认候选 workflow 仍只有 run `33246312135` / attempt 1、artifact `9712952210` 仍存在且未过期，远程 `master` / `dev` 未漂移；该 pending registration 切片已提交为 `a032fcb520d50283ef24391d14869c3bbfcbebb3`，尚未推送。
20. 对 Checker 远程只读复核确认当时 tag 与 Release 集合均为空；通用 repository REST 响应不提供 immutable releases 设置状态，当日没有使用后续公开的专用 endpoint，因此不能把 provider 选择写成设置已经启用。没有修改仓库设置、Ruleset 或任何其他远程状态。
21. 接受 [ADR 0010](../adr/0010-checker-runtime-payload-durable-registration.md)：首个 durable provider 选择 Checker 仓库的 GitHub immutable Release asset；每个 source / implementation version / target 使用一个专用 `checker-payload/...` tag 与唯一 distribution asset，禁止 `latest` alias。登记状态按 candidate → distribution accepted → durable published → registered inactive → active 单向推进，已发布记录只允许 append-only 撤销 / replacement，provider attestation 不能替代项目原始摘要、payload / distribution acceptance 或独立回读。
22. 当前候选 USTAR 只含 binary、build provenance、payload acceptance 与 retention manifest，且 acceptance 明确排除 distribution legal compliance 与 publication，因此不能原样发布。下一实现前置被冻结为自包含 `radishaxiom-checker-runtime-distribution` `0.1` 外层 USTAR：绑定内层候选、distribution manifest、checker Apache-2.0 `LICENSE` 与 Go `LICENSE` / `PATENTS`，并形成独立 distribution acceptance；实现会改变 `checker.source`，之后必须产生新候选。
23. Checker Runtime Payload Registration v0.1 已把 durable provider、分发前置、状态机与当前 `blocked-prerequisites` 写入生成契约；新增临时候选直接 active、可变 Release 登记、`latest` alias、release attestation 冒充 acceptance、缺少独立回读与原地 replacement 六类负例。专项生成复核通过 17 个文件，`git diff --check` 通过，仓库门禁通过 887 个文件；本切片不执行 push，没有创建 tag / Release、上传 asset、激活 runtime 或删除候选 artifact。
24. Checker `dev` 在远程共同基线 `a81f5b4704efddd1d8c293f9e8e47c58149e65b7` 上实现 distribution package / verifier 与独立 distribution acceptance：acceptor 精确绑定 `go1.26.7.darwin-arm64.tar.gz`、Go `LICENSE` / `PATENTS`、Checker Apache-2.0 `LICENSE` 和内层候选，形成闭合 acceptance root；packer 形成含内层候选、distribution acceptance / manifest 与三份法律材料的 6-member 确定性 USTAR，并严格重建回读。手工候选 workflow 已扩展为 build → payload acceptance → inner pack → distribution acceptance → outer pack → direct-file 上传 / 按 ID 回读。实现提交为 `f6a02b9314051fd841e1f3d3d1491a8a73ad7da7`，tree 为 `837ed9bae4b47e8fd4001829e0dd77d70f0ea36f`；Checker source sidecar 已重放为 703 个文件、187,178 bytes 与 `sha256:e8a2991664c701b92d2020bf72b26d013034b1f3fc75270f49511be319fd2625`。以本机 `go1.26.3`、`GOTOOLCHAIN=local`、`CGO_ENABLED=0`、`GOPROXY=off` 和隔离 cache 运行全量 test / vet、source identity、module 闭包、YAML / shell syntax、gofmt、治理与差异卫生均通过。经单独授权，远程写前确认 `master` / `dev` 均为 `a81f5b4...49e65b7`，随后只将 `dev` 快进到 `f6a02b9...ad7da7`；写后读取确认 `master` 未变。push 触发的常规 `Checker Checks` run `33253582957` / attempt 1 在精确 head 上成功，`Repository Governance` job `99103339330`、`Checker Go Quality` job `99103339478` 与 `Candidate Quality` job `99103396871` 均为 `success`；该 commit 的 `Checker Payload Candidate` run 集合为空。这不是精确 `go1.26.7` 受控构建或 production distribution acceptance；旧 `e2c4ae...9044` Actions artifact 记录不被改写，active runtime 保持 0，且没有修改 `master`、Ruleset、tag / Release、上传候选、安装或激活。
25. Checker 治理提交 `12bbd5b4c14583e625ec6c242d5cdf7fe6d9ba1d` 将常规 workflow 收窄为 PR / 手工入口，普通 `dev` / `master` push 不再自动触发 CI。写前门禁发现该提交遗漏 source sidecar 重放并以 `source manifest drift` 失败，因此没有直接推送或 amend；更新 sidecar 后当前 source 为 703 个文件、187,178 bytes 与 `sha256:401158c3c304f45faebebe879edf064512998423d7b08aec486f4be0012e3999`，全量 test / vet、source identity、module 闭包、YAML / shell syntax、gofmt、治理与差异卫生重新通过。独立补正提交为 `4b95b2a81616110f5d3ed076f882a18ddc6aba37`，tree 为 `fb542000aec53bd9d6c95e55cf1abfcbd9d91b20`。经单独授权，两笔提交从 `f6a02b9...ad7da7` 精确快进推送到 `origin/dev = 4b95b2a...aba37`；写后读取确认 `origin/master` 仍为 `a81f5b4...49e65b7`，该 head 的 push run 集合为空，符合新治理。候选 workflow 未运行，主仓未推送，Ruleset、tag / Release、artifact 与 active runtime 均未修改。
26. 经单独授权创建并审查 Checker `dev -> master` PR `#2`：远程写前确认默认分支 / `master = a81f5b4704efddd1d8c293f9e8e47c58149e65b7`、`dev = 4b95b2a81616110f5d3ed076f882a18ddc6aba37`，且同目标 open PR 集合为空；PR 精确包含预期 3 个提交与 18 个文件。`Checker Checks` run `33255345832` / attempt 1 / `pull_request` 在精确 head 上成功，`Repository Governance` job `99107948512`、使用精确 `go1.26.7` 的 `Checker Go Quality` job `99107948632` 与聚合 `Candidate Quality` job `99108008168` 均为 `success`；合并前 PR 状态为 `OPEN / MERGEABLE / CLEAN`，该 head 的 `Checker Payload Candidate` run 集合为空。该审查切片没有修改 `master`、候选 artifact、Ruleset、tag / Release、active runtime 或主仓远程。
27. 经单独授权，PR `#2` 以 merge commit `f960603aa1120ebe427eb9227f116f4a41513d5e` 合并；其第一父提交精确为旧 `master = a81f5b4...49e65b7`，第二父提交精确为已审查 `dev = 4b95b2a...aba37`，tree `fb542000aec53bd9d6c95e55cf1abfcbd9d91b20` 与 PR head 完全相同。只读写后确认 PR 状态为 `MERGED`，随后仅将远程 `dev` fast-forward 到同一 merge commit，并以 `--ff-only` 同步本地 Checker `dev`；最终 `origin/master` / `origin/dev` / 本地 `dev` 均为 `f960603...13d5e`，祖先差异为 `0 / 0`。merge head 的普通 `Checker Checks` 与 `Checker Payload Candidate` run 集合均为空，仓库治理和 source identity 复核通过；没有修改 Ruleset、tag / Release、artifact、active runtime 或主仓远程。
28. 当日收口复核覆盖主仓 13 笔提交与 Checker 8 笔提交（含 2 个 merge commit），逐项对照 distribution packer / verifier / acceptor、workflow、Checker distribution / retention 专题、ADR 0010 和 runtime payload 登记契约。6-member 顺序、三份法律材料原始身份、acceptance scope、归档命名、45 + 20 分钟 job timeout、90 天 direct-file 上传与按 artifact ID 回读保持一致；Checker 稳定文档无需修改，因此没有再次改变 `checker.source`。主仓原先把 `e2c4ae...9044` 称为当前 source、把 distribution 写成尚未实现的时间性漂移已在文档入口、ADR 背景、契约摘要和本状态中修正；生成 pending record 继续精确保存旧 source 的既有候选，不能在新 workflow 产出前预填当前 source 的未知 artifact / acceptance / provider 字段。

停止线：Ruleset 尚未获得本任务授权，不因 `Candidate Quality` 已出现而自动写入；后续若启用必须先读取远程现状、形成精确策略并单独授权。不安装或替换系统 Go，不自动下载本地工具链或 binary，不提交 executable，不创建 Release / 部署，不检查 minimality，不增加非空 kernel / certificate 能力，不执行 solver、生产 Node target、生产编译器或正式模型调用，也不宣称 release、产品安装或六平台通过。

## 今日进展（2026-08-30）

1. 启动时按根入口复核双仓状态：主仓 `dev` 与 `origin/dev` 同步且工作树干净；Checker 本地 `dev`、远程 `master` / `dev` 均精确为 `f960603aa1120ebe427eb9227f116f4a41513d5e`，工作树干净，当前 source sidecar 为 703 个文件、187,178 bytes 与 `sha256:401158c3c304f45faebebe879edf064512998423d7b08aec486f4be0012e3999`。远程只存在旧 head 的候选 run，当前 head / source 尚无同输入候选；没有 reset、清理或重写历史。
2. 经所有者对本次唯一远程写入的明确授权，在精确 Checker `master` 触发 `Checker Payload Candidate` run `33301288846` / attempt 1，输入 `source_identity = sha256:401158...e3999`、`version = 0.1-dev` 与显式上传确认。run 精确绑定 head `f960603...13d5e`，没有重跑；构建 / 验收 / 分发 / 上传 job `99229841973` 与 Ubuntu 独立回读 job `99229998823` 均为 `success`。
3. 当前 source 的两次隔离构建形成原始字节一致的 4,689,378-byte binary `sha256:7e2816...5aaf`；1,389-byte canonical provenance 为 `sha256:45d0ef...2404`，1,580-byte canonical payload acceptance 为 `sha256:575705...7b55`，决定为 `accepted-for-controlled-runtime-registration`。三条真实 CLI 结果继续分别为 `accepted-with-trust`、`rejected` 与 `incomplete`，并绑定本次 binary / source 身份。
4. 内层 4,697,600-byte candidate USTAR 为 `sha256:fb3364...4f3ea`，1,295-byte retention manifest 为 `sha256:6420a6...143a`。独立 distribution acceptance 为 1,869 bytes / `sha256:6f3e6d...ca034`，决定为 `accepted-for-controlled-durable-publication-candidate`；1,721-byte distribution manifest 为 `sha256:b4c95c...c36ad`，外层 6-member、4,720,640-byte distribution USTAR 为 `sha256:17b44a...fa437`。
5. 外层 distribution 以 `archive: false` 直传为 GitHub Actions artifact ID `9729031154`；provider 元数据记录创建时间 `2026-08-30T08:20:28Z`、到期时间 `2026-11-28T08:19:14Z`、4,720,640-byte size 与同一 SHA-256。独立 Ubuntu job 按精确 artifact ID 原样下载，并通过 provider metadata、外层 distribution、内层 candidate 和全部身份回读；本地随后也按同一 artifact ID 取得原始 USTAR，并由 Checker 严格 verifier 重放，两级输出与远程完全一致。本地附加复核由 `go1.26.3` 运行，只补充字节检查，不替代远程精确 `go1.26.7` 的 acceptance。
6. Checker Runtime Payload Registration v0.1 的 current pending record 已只按实际输出更新为 `distribution-package-accepted`，保留 active runtime 为 0、临时候选最长 90 天和 durable provider 尚未物化的边界；生成器同步重算 record / contract digest、闭合 schema 与负例。专项生成复核通过 17 个文件，仓库门禁通过 887 个文件，`git diff --check` 通过。该状态不是 Release、durable publication、正式登记、安装或 runtime 激活。
7. 本切片没有修改 Ruleset / repository setting，没有启用 immutable releases，没有创建 tag / Release、安装或激活 runtime、推送主仓或删除旧 provider artifact。本地隔离下载副本已在复核后删除；immutable setting、Release、正式登记、launcher OS 隔离与产品安装继续作为分别复核、分别授权的后续切片。
8. 按所有者明确要求，精确暂存本次 17 个登记、生成与状态文件，在生成复核、仓库门禁和 staged diff 卫生再次通过后提交为 `1dd5346f41282515be02171d709c10aebc1c6905`，tree 为 `09a16999dc93c739dd0864f6b2afefb5b2194df5`；提交后主仓 `dev` 比 `origin/dev` 超前 1，没有 push。
9. 下一顺位只读治理核验使用 GitHub API `2026-03-10` 的 `GET /repos/laugh0608/RadishAxiomChecker/immutable-releases` 专用 endpoint；当前凭据对仓库为 `ADMIN`，响应 `enabled = false`、`enforced_by_owner = false`，精确响应时间为 `2026-08-30T08:36:33Z`。远程 tag 与 Release 集合均为空；通用 repository 对象缺失该字段形成的 `null` 没有再被误写为设置状态。本步没有调用启用 endpoint 或修改任何远程状态。
10. 精确下一发布计划绑定 target commit `f960603...13d5e`、tag `checker-payload/go0.1-dev/darwin-arm64-v8.0/sha256-401158...e3999`、名称 `RadishAxiom Checker go0.1-dev darwin-arm64-v8.0` 与唯一 4,720,640-byte / `sha256:17b44a...fa437` distribution asset。设置启用及回读必须先单独授权；draft 创建 / asset 上传 / draft 字节复核 / 单次 publish 是之后另一授权。发布后必须同时得到 REST `immutable = true`、`gh release verify`、`gh release verify-asset`、精确 asset metadata / 原始字节回读以及外层 distribution / 内层 candidate 严格验证，才可更新为 `durable-published`。
11. 经所有者对这次精确 repository setting 写入的单独授权，对 `laugh0608/RadishAxiomChecker` 调用 API `2026-03-10` 的 `PUT /immutable-releases`，于 `2026-08-30T08:43:23Z` 得到 `204 No Content`；随后同一专用 endpoint 于 `2026-08-30T08:43:30Z` 回读为 `enabled = true`、`enforced_by_owner = false`。`2026-08-30T08:44:13Z` 再次复核远程 tag 与 Release 集合仍为空。本步没有创建 tag、draft、Release 或上传资产，也没有扩大到已经明确分开的 publication 授权。
12. 经所有者对完整 publication 切片的独立授权，先按 artifact ID `9729031154` 取得原始 distribution，并以本机 `go1.26.3` / `GOTOOLCHAIN=local` 严格复核外层、内层、acceptance、manifest 与六成员 inventory；随后创建 draft Release ID `379226889`，只上传 asset ID `536372439`。draft 的 asset API metadata 与重新下载字节均精确为 4,720,640 bytes / `sha256:17b44a...fa437`，再次通过严格 verifier 后才执行唯一一次 publish。发布于 `2026-08-30T08:53:07Z` 完成，REST 为 `draft = false`、`immutable = true`，tag 精确解析到 `f960603...13d5e`，asset 保持 `uploaded` 与相同长度 / digest。create / publish 请求均显式使用 `make_latest = false`，但 GitHub 按首个 Release 的平台规则仍报告 `isLatest = true`；这一可变展示标签没有进入登记，`latest` / `releases/latest` / `latest/download` 继续禁止作为身份或 fetch。
13. GitHub Release / asset attestation 均由 `gh 2.96.0` 验证通过：predicate type 为 `https://in-toto.io/attestation/release/v0.2`，签名 SAN 为 `https://dotcom.releases.github.com`，可信时间戳为 `2026-08-30T08:53:08Z`，subjects 分别绑定 tag commit 与唯一 distribution digest。默认命令因沙箱不能写 `~/.cache/gh/.sigstore` 而无法初始化 verifier；将 `gh attestation trusted-root` 取得的 GitHub instance root 放入隔离临时目录并通过隐藏的 `--custom-trusted-root` 失败关闭地重跑后成功，没有安装或升级 CLI。发布后 authenticated CLI 与公开 browser URL 两条路径下载的字节逐字节相同并再次通过严格 verifier；隔离临时副本将在登记复核后删除。该结论只推进到 `durable-published`，active runtime 仍为 0。
14. 经所有者对 `durable-published -> registered-inactive` 的明确授权，主仓再次复核生成契约、Release ID `379226889`、asset ID `536372439`、`immutable = true`、唯一长度 / digest 与 tag → target commit 绑定均未漂移。当前记录于 `2026-08-30T09:21:33Z` 进入 `registered-inactive`，状态计数、runtime statement、schema 与负例同步收口，并将不再准确的 `current-pending` 路径和两条状态相关负例改为正式 inactive 命名。该转换只说明主仓已绑定持久字节与重新验证路径，不安装、不激活、不允许 launcher 选择，也没有修改 Checker 远程状态或推送主仓。
15. 接受 [ADR 0011](../adr/0011-checker-runtime-launcher-installation-and-activation.md)，并在既有 runtime payload 生成入口内物化唯一 canonical `launcher-policy.jcs`、闭合 schema 与 8 条负例。策略精确区分产品 active-only 选择和 registered-inactive qualification，固定 `darwin / arm64 / v8.0 / macho-64-arm64` 无 fallback 目标键、content-addressed atomic install、installation receipt / qualification record、三条既有 companion 精确重放、每次 spawn 前后 binary 身份复核，以及 process failure 不形成 `incomplete`。集合契约新增 activation readiness 并保持 blocked，既有 registered-inactive 记录字节不作追溯修改：policy 仍是 `specified-not-implemented`，installation、qualification companion 与激活授权均未物化，active count 保持 0。本切片没有下载、安装或执行 payload，没有修改 Checker 仓库或其他远程状态。
16. 新增 dependency-free Python installer / launcher 一致性核心和仓库级入口，以 canonical policy 与 inactive record 为唯一身份输入；16 个合成测试覆盖 active / inactive 选择分流、未知 target / Rosetta、危险 target path 与重复记录失败关闭、严格 USTAR 路径 / type / mode / digest / order / trailer、canonical installation receipt、Mach-O arm64 binary 身份、持锁同文件系统 publish、既有 slot 精确复用与不覆盖、三条已通过外部 contract parser 的 qualification companion 绑定 / exclusive record、摘要闭合但字段缺失的 qualification record 拒绝，以及 process / identity failure 不形成 checker 四态。所有文件操作只发生在测试临时目录，未读取网络、真实安装根或 payload；该核心不实现两层真实 distribution manifest parser、完整 Independent Check result parser、原生进程 / sandbox / memory enforcement 或生产 launcher，故 policy 继续为 `specified-not-implemented`，active 仍为 0。
17. 接受 [ADR 0012](../adr/0012-product-checker-runtime-host-and-persistence-interface.md)，并把实现宿主、网络能力、持久化接口和 result consumer 隔离边界加入 canonical launcher policy：产品 runtime 使用与 `raxc` 同发布图的 Rust 2024 / 精确 `1.97.1` 内部组件，core 无网络，store root 只由产品注入，六项能力固定为 target lock、owned staging、exclusive slot publish、exact slot read、exclusive qualification 与 append-only attempt；qualification / invocation 共用主仓 Rust consumer，但不得复用 Checker Go parser 或以 Python oracle 作为产品依赖。由于这些都是闭合必需字段，policy 从 pre-implementation `0.1` 显式升级到 `0.2` / 新摘要域，登记集合与既有 inactive record 仍保持 `v0.1` 字节不变且不回退旧 policy。新增 6 条机器负例与 3 项一致性测试，专项生成复核通过 34 个文件、launcher 核心通过 19 个测试；本切片未创建 Cargo workspace、工具链文件、依赖声明或 `Cargo.lock`，未下载、安装或执行真实 payload，也未改变 active 计数。
18. 完成 [Checker runtime 首个 Rust 纵向切片审阅](../checker-runtime-rust-first-slice-review.md)：官方 Rust `1.97.1` macOS arm64 standalone archive 与 source archive 的 publisher SHA-256 已捕获到 Toolchain Registry，但仍未下载 / 验收；同时确认 registry standalone 与拟议 rustup component 是不同字节链，安装前必须先统一并登记实际路径。`sha2 = "=0.11.0"` / `default-features = false` 的 macOS arm64 当日候选闭包静态审阅到 9 个 `MIT OR Apache-2.0` package，其中 `cpufeatures -> libc` 引入 platform FFI 与 `build.rs`；首切片因此收敛为一个 `publish = false` 的内部 library crate、零 production / dev dependency、自有 safe SHA-256、闭合 canonical JSON 子集、policy / registered-inactive 身份重算和 qualification / product selection。registry 新身份已依序重算 toolchain acceptance、pipeline、readiness、execution profile、558-file checker bundle 与 launcher policy 绑定；7 个生成入口共 713 个文件复核通过，launcher 核心 19 个测试和暂存后的 913-file 仓库门禁通过。临时 crate 字节只在隔离目录做 checksum / manifest / 静态检查后删除，未执行；本切片没有创建 Cargo 文件或 Rust 源码，没有安装工具链、生成 lockfile、下载真实 checker、修改用户级状态或远程状态。
19. 经所有者对工具链 payload acceptance 的精确授权，选择 rustup component 路径并登记 `channel-rust-1.97.1.toml`、`minimal` profile、`aarch64-apple-darwin` target、`rustc` / `cargo` / `rust-std` 与显式 `rustfmt` / `clippy`。隔离下载的五个 component 与 source 共 353,753,208 bytes，整包 SHA-256 均与 publisher manifest / sidecar 匹配；连接不完整分段先被拒绝，补齐后才组装。只读检查确认 component 无 link / hardlink / 特殊文件，source 的 323,914 个 member、148 个 symlink 均停留在归档顶层，并记录 4,613 份 `Cargo.toml`、1,977 份 `Cargo.lock` 与两套各 12 种 SPDX 表达式库存。八份工具链 acceptance record（含既有两份 Go）、registry / schema / contract 与审阅单同步更新；下游 pipeline / readiness / execution profile / 558-file checker bundle / launcher policy 摘要链依序重算，暂存后仓库门禁通过 925 个文件，3 份 Draft 2020-12 schema 验证全部 17 份对应实例。隔离目录的 757 MiB 实际占用已精确删除；Rust tool 只升级为 `partial-accepted-set-only`，standalone 仍未接受，安装 / 执行 / workspace / lockfile / 具体分发法律结论均未授权。
20. 经所有者对用户级工具链安装的独立授权，rustup 以 `minimal` profile 和显式 `rustfmt` / `clippy` 安装 `1.97.1-aarch64-apple-darwin`。下载器低速期间复用既有验收方法，以官方 HTTPS URL 的连续 Range 在隔离临时目录补齐 `rustc`、`rust-std` 与 `rustfmt`；五个组件整包 SHA-256 再次与已接受记录逐字匹配后才进入 rustup 缓存。干净安装最终只登记 `cargo`、`clippy`、`rust-std`、`rustc`、`rustfmt`，工具版本分别为 `cargo 1.97.1`、`clippy 0.1.97`、`rustc 1.97.1` 与 `rustfmt 1.9.0-stable`；默认工具链仍为 `1.96.0`。中断残片与任务临时目录已删除，没有创建 Cargo 文件、运行项目代码、修改 shell / PATH、安装 checker runtime 或改变 active 状态。
21. 日终复核主仓今日 10 笔提交，逐项对照 runtime payload 登记生成器 / 契约、launcher policy、Python 一致性核心、ADR 0011 / 0012、工具链 registry / acceptance、Rust 首切片审阅单与文档入口。机器 schema、canonical fixture、摘要链、19 项 launcher 测试边界和代码说明保持一致，没有发现需要修改 Axiom IR、Axiom Evidence、Independent Check Contract、launcher policy 公共格式或生成器行为的漂移；需要修正的仅是工具链安装后的时间性状态与下一顺位。验收契约中的 `installation = not-authorized` 保留为该验收批次本身不授予安装权限的历史边界，不把单台开发主机的可变 rustup inventory 写入公共摘要链。
22. 日终文档收口后，toolchain adapter identity、toolchain payload acceptance 与 checker runtime payload 三项生成复核分别通过 2、19、34 个生成文件，launcher consistency 通过 19 项测试，仓库门禁通过 925 个文件，`git diff --check` 通过。ADR 0012 的原始字节继续保持不变：其工具链语句是决策当时的历史前提，且已进入 runtime payload 身份链；当前主机状态只更新审阅单和阶段状态，不触发公共契约摘要重算。

## 今日进展（2026-08-31）

1. 启动时复核主仓 `dev` 与 `origin/dev` 同步且工作树干净；`1.97.1-aarch64-apple-darwin` 的 `cargo`、`clippy`、`rust-std`、`rustc`、`rustfmt` 五组件仍完整，`rustc 1.97.1 (8bab26f4f 2026-07-14)` 与 `cargo 1.97.1 (c980f4866 2026-06-30)` 精确匹配既有验收。用户默认和当前任务环境覆盖仍为 `1.96.0`，没有修改用户默认工具链、shell、PATH 或系统配置。
2. 创建根 `rust-toolchain.toml`、resolver `3` virtual workspace `Cargo.toml` 和由精确 `cargo 1.97.1` 生成的 version 4 `Cargo.lock`；唯一成员是 `publish = false`、Rust 2024、版本占位为非发布 `0.0.0` 的内部 library `radishaxiom-checker-runtime`。production / dev dependency、feature、binary、build script、proc macro 和 native code 均为 0，没有预建 `raxc`、公开 CLI、provider client 或平台 facade。
3. 自有 `sha256` 模块只暴露 crate 内字节摘要能力，使用 safe Rust、无 SIMD / CPU detection / FFI / `unsafe`；测试覆盖空串、`abc`、55 / 56 / 63 / 64-byte padding 边界、million-`a`，并通过真实 policy / record 的既有 Python 域摘要形成跨实现观察。
4. 自有 strict JSON 层只接受当前 policy / record 需要的 ASCII string、boolean、array 与 object，拒绝 duplicate、非规范 member 顺序 / escape、number、`null`、非 ASCII、尾随字节和 compact policy 的空白漂移；policy 与 current registration 的全部嵌套 object key / value kind 由闭合形状清单检查，未知或缺失 member 在摘要正确时仍失败关闭。
5. `LauncherPolicy` 与 `RegistrationRecord` 分别重算 `radishaxiom.checker-runtime-launcher-policy.v0.2` 和 `radishaxiom.checker-runtime-payload-registration.v0.1` 域摘要，提取 checker source / version / toolchain、binary、distribution、registration status 与完整 native target。`NativeProcessMode`、`SelectionPurpose` 和 `RegistrationStatus` 保持类型分离；qualification 只选择一个精确 inactive record，product 只选择一个精确 active record，translated process、unknown architecture / variant、inactive product、重复 target 和 ambiguous selection 均返回 typed `runtime-unavailable`，不 fallback。
6. 以显式 `+1.97.1-aarch64-apple-darwin` 运行 Cargo lockfile generation、`fmt --check`、`clippy --workspace --all-targets --all-features -- -D warnings` 和 13 项 `cargo test --workspace --all-targets`，全部通过。locked / offline metadata 只含一个 Rust 2024 library package、一个 workspace member、零 dependency / feature；locked / offline dependency tree 也只有该 crate。
7. 既有 Python launcher oracle 19 项测试、checker runtime payload 34 个生成文件和 `./scripts/check-repo.sh` 的 935 文件门禁全部通过；仓库级 required-file 清单已纳入 toolchain、workspace、lockfile 与六个 Rust source。当前没有修改 machine policy、schema、registration record、ADR 或公共格式，也没有下载 / 安装 / 执行真实 checker distribution。
8. 本切片未创建后台服务或临时外部状态，没有安装依赖、写入远程、push、发布或激活 runtime。完整 launcher policy 继续为 `specified-not-implemented`，active runtime 保持 0；这 13 项 Rust 测试只证明当前实现满足所测的闭合 policy / record / selection 边界，不构成形式证明、runtime companion、真实安装或跨平台证据。

## 下一事项

1. 先审阅并收口本次 Rust workspace / identity / selection 切片；下一连续实现从 strict outer / inner archive inventory、installation receipt 与 store transaction 中选择一个仍可独立验证的真实切片，不一次混入 result consumer、subprocess 或平台资源隔离。
2. 后续若需要第三方 crate、build script、proc macro、native code、网络能力或新的平台适配，先重新完成依赖 / 许可证 / 供应链审阅并取得对应授权；不因为本次零依赖决定而默认允许或永久禁止未来依赖。
3. 真实 immutable asset fetch / install、checker payload 执行、产品绝对安装根、系统隔离验证和 `registered-inactive -> active` 继续分别验证、分别授权；当前不修改远程状态且不 push。

## 后续顺位

1. Go host/source 与 Rust rustup component/source 局部供应链门禁、checker request / bundle parser、`checker.source` v0.1、当前锁定 Axiom IR 结构 / 类型良构与有限执行、Axiom Evidence 结构 / 身份、obligation completeness、state / support、counterexample world / `WF`、concrete input / `Pre`、8 个 proof-failure target replay、host / golden output comparison、proof support 真值边界、production conclusion 精确重算、内存四态结果、canonical companion codec、invocation failure envelope、完整 invocation / 累计资源、唯一产品 CLI、checker 公开远程仓库 / 分支 / CI 基线、runtime payload 登记契约、确定性候选 / distribution 归档，以及当前 `401158...e3999` source 的精确受控构建、payload / distribution acceptance、候选直传 / 按 ID provider 回读、repository immutable setting、精确 draft / 单次发布、attestation、发布后双路径回读、`registered-inactive` 登记、launcher policy、纯本地一致性验证核心、产品侧实现宿主 / 持久化接口冻结、精确用户级 `1.97.1-aarch64-apple-darwin` 五组件安装，以及零第三方依赖的单 crate Cargo workspace / toolchain pin / lockfile、safe SHA-256、strict policy / current record identity 与 target selection 首切片已经通过，active runtime 保持 0。strict archive / receipt、store transaction、result consumer 和 process / identity 失败矩阵保持为后续真实切片；本机默认或环境覆盖的 `1.96.0` 不得替代仓库冻结工具链。真实 checker distribution 下载 / 安装、payload 执行、系统隔离验证和 `registered-inactive -> active` 仍分别验证和授权。immutable Release、Git 对象 ID、CI success、provider attestation、distribution acceptance、inactive 登记、policy schema、审阅单、工具链本机安装、首个 Rust selection 切片或 Python 一致性模型继续不能单独冒充 active runtime 或产品发布。
2. 只有独立远程仓库 / CI 基线、精确 `go1.26.7` 确定性构建、source → artifact 映射、独立 payload acceptance、主仓登记、launcher 实现、真实安装与 qualification 连续通过，才能形成正式 runtime companion；产品安装、依赖安装、激活、发布与部署仍分别提醒并授权。
3. cvc5、Node、Rust standalone / 其他平台 payload 与六平台原生结果按实现依赖逐批验收；certificate profile 只有在格式、checker、完整规则覆盖和 trust step 政策独立通过后才可加入非空支持集合。
4. 工具链可用且实现入口验证通过后，才准备 Agent 实验 execution lock 和正式模型调用。

首域语义、Axiom IR、Axiom Evidence 与较早 ADR 中“后续技术决策尚未冻结”的文字属于其接受时的范围说明；现行实现语言、验证后端、目标执行、生产管线和独立 checker 口径分别以 ADR 0004–0008 为准，runtime payload / launcher / 产品宿主边界以 ADR 0010–0012 为准。首域语义的原始摘要已被基准语料和 Agent 实验注册绑定，Axiom IR 规范的原始摘要也已被实验注册绑定，不能为同步阶段措辞而原地改写。

## 尚未冻结

- 表面语法；
- Axiom Evidence 的具体证明 certificate 格式、非空 kernel / certificate 能力，以及独立 checker `master` Ruleset、正式 runtime companion、产品绝对安装根 / 权限适配和 launcher / installer 的 archive、receipt、store、result consumer、spawn / isolation 原生实现；
- Rust standalone / 其他平台、cvc5 / Node、其余 Go 平台 payload 的实际摘要 / 签名验收、包内依赖与许可证清单，Go host/source 的 publisher 签名与源码可复现性，首个受支持 certificate 格式 / checker / 规则覆盖，以及真实跨实现 / 跨平台语义结果；
- Agent 实验的 execution lock、模型精确 revision、提示材料和 runner；
- 包管理、IDE、插件和发布载体；
- 首个具体产品版本、发布载体、发布记录与自动化；
- v1 后语言语义、Axiom IR、Axiom Evidence 及未来公共包的兼容性承诺。

在上述决策完成前，不为占位目的引入完整编译器骨架、运行时依赖、自动发布、CODEOWNERS 或技术栈专属 CI。

## 按需阅读

- [产品定义](../product-definition.md)
- [许可证与生态策略](../licensing-strategy.md)
- [仓库治理](../governance/repository-governance.md)
- [ADR 0001：分支、PR 与 Ruleset 治理](../adr/0001-branch-and-pr-governance.md)
- [ADR 0002：首个目标领域与基准任务](../adr/0002-first-target-domain-and-benchmarks.md)
- [ADR 0003：版本标识与兼容性分层](../adr/0003-version-identities-and-compatibility-layers.md)
- [ADR 0004：`raxc` 生产编译器实现语言](../adr/0004-raxc-production-implementation-language.md)
- [ADR 0005：首个验证后端与失败关闭边界](../adr/0005-first-verification-backend.md)
- [ADR 0006：首个目标运行时与执行路径](../adr/0006-first-target-runtime-and-execution-path.md)
- [ADR 0007：首版验证优先编译管线与制品协议](../adr/0007-first-verification-first-compilation-pipeline.md)
- [ADR 0008：独立 checker 的实现语言、制品交换与隔离边界](../adr/0008-independent-checker-isolation-and-artifact-exchange.md)
- [ADR 0009：Axiom Evidence v0.1 漂移收口与 v0.2 迁移边界](../adr/0009-axiom-evidence-v0-drift-and-migration.md)
- [ADR 0010：独立 checker runtime payload 的持久发布与登记](../adr/0010-checker-runtime-payload-durable-registration.md)
- [ADR 0011：独立 checker runtime launcher、安装与激活边界](../adr/0011-checker-runtime-launcher-installation-and-activation.md)
- [ADR 0012：产品侧 checker runtime 宿主与持久化接口](../adr/0012-product-checker-runtime-host-and-persistence-interface.md)
- [Independent Check Contract v0.1](../../contracts/independent-check-v0.1/README.md)
- [Execution Profile Contract v0.1](../../contracts/execution-profiles-v0.1/README.md)
- [Toolchain & Adapter Identity Registry v0.1](../../contracts/toolchain-adapters-v0.1/README.md)
- [Checker Runtime Payload Registration v0.1](../../contracts/checker-runtime-payloads-v0.1/README.md)
- [Pipeline Artifact Contract v0.1](../../contracts/pipeline-artifacts-v0.1/README.md)
- [Implementation Readiness Contract v0.1](../../contracts/implementation-readiness-v0.1/README.md)
- [Keyed Finite Table Checker Bundle Contract v0.1](../../contracts/keyed-finite-table-checker-bundles-v0.1/README.md)
- [有键有限表转换：首版类型化语义](../semantics/keyed-finite-table-semantics.md)
- [Axiom IR v0.1：规范化形式与版本策略](../ir/axiom-ir-v0.md)
- [Axiom Evidence v0.1：证据模型与独立检查边界](../evidence/axiom-evidence-v0.md)
- [有键有限表基准语料库 v0.1](../benchmarks/keyed-finite-table-corpus-v0.md)
- [Agent 表示与验证反馈对比实验预注册 v0.1](../experiments/agent-representation-preregistration-v0.md)
- [面向 Agent 的语言设计：证据与开放问题](../research/agent-oriented-language-design-evidence.md)
