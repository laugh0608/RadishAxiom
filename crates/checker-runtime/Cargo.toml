[package]
name = "radishaxiom-checker-runtime"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]

[target.'cfg(target_os = "macos")'.dependencies]
radishaxiom-checker-runtime-darwin-store = { path = "../checker-runtime-darwin-store" }

[dev-dependencies]
