# 安全策略

RadishAxiom 仍处于预发布定义阶段，当前没有承诺长期安全支持的稳定版本。但编译器健全性、验证结论、信任边界和 Evidence 可伪造性从项目早期开始就按安全问题处理。

## 私下报告

请不要为未修复漏洞创建公开 Issue，也不要在报告中附带真实密钥、个人数据或生产凭据。

优先使用 GitHub 仓库 Security 页面中的 Private vulnerability reporting。若该入口不可用，可发送邮件至 `laugh0608@foxmail.com`，主题包含 `[RadishAxiom Security]`。

报告应尽量包含：

- 受影响的提交、版本、模块或格式；
- 可复现输入、最小示例或 Evidence；
- 预期保证与实际行为；
- 对机密性、完整性、可用性或验证健全性的影响；
- 已知利用条件、临时缓解和建议修复方向；
- 是否可以公开致谢及希望使用的署名。

当前阶段按 best-effort 方式确认和处理报告，尚不承诺固定响应 SLA。公开时间应由报告者与维护者协调，避免在修复或缓解措施可用前披露可直接利用的细节。

## 视为安全问题的范围

- 将未证明或未检查的性质错误标记为 `proved` / `checked`；
- 遗漏、隐藏或错误传播 `trusted` 假设与外部能力；
- 可伪造、替换、截断或错误归属的 Axiom Evidence；
- 解析、规范化、类型、效果或验证器之间产生可利用的语义差异；
- 独立检查器接受无效证据，或与生产验证器结论不一致；
- 恶意输入导致任意代码执行、越权 I/O、路径穿越、拒绝服务或资源失控；
- 依赖、构建、发布、插件、求解器或生成产物的供应链风险；
- 凭据、隐私数据、专有输入或验证材料泄露。

普通拼写、无安全影响的文档错误、已知非安全兼容问题和不涉及信任结论的功能建议可以使用公开 Issue。
