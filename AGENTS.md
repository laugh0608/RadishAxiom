# RadishAxiom 协作约定

本文件为人工协作者与 AI 协作者提供统一、长期稳定的工作规则。它只约束 RadishAxiom，不复用 Radish 家族其他项目的实现边界。

## 称呼与语言

- 对话开始或结束总结时称呼用户为 `萝卜SAMA`。
- 默认使用中文讨论、说明和编写项目文档。
- 代码、命令、路径、配置键、类型名、协议字段和外部项目名保留原文。
- 英文工程术语在中文可以准确表达时优先使用中文；不能准确翻译的术语首次出现时可附英文。

## 开始任务前

1. 检查 `git status`；若实际分支、ahead / behind、暂存区或工作树与用户描述的基线不一致，先报告差异，不用 reset、checkout 或清理命令制造预期状态。
2. 区分本次改动、用户已有改动和可能的并行改动，只把任务范围内文件纳入修改、验证和提交。
3. 只读取与任务直接相关的最小文档链，先确认现行口径，再决定是否需要追溯历史材料。
4. 明确任务属于说明、诊断、设计、实现、治理还是发布，不把一种授权扩大成另一种操作。
5. 识别是否影响正式语义、信任边界、公共格式、兼容性、许可证、依赖、用户数据或远程仓库设置。
6. 先查找现有实现、测试、脚本和命名惯例；没有充分理由时不另造平行入口、重复 helper 或第二套配置口径。

默认阅读顺序：

1. `docs/status/current.md`
2. `docs/README.md`
3. 与任务相关的产品定义、ADR、治理或专题文档

询问“下一步做什么”时先读 `docs/status/current.md`；不要默认展开全部历史、周志或无关兄弟项目。

## 协作流程

- 用户明确要求修改且范围清楚、风险可控时，直接实施并完成风险匹配的验证。
- 用户只要求分析、解释、审查或建议时，不擅自修改仓库或 GitHub 远程状态。
- 需求不明确，或将改变架构、语义、信任、许可证、兼容性或外部状态时，先说明关键判断；只有缺失选择会实质改变结果时才停下询问。
- 授权只覆盖当前任务中已经说明的动作、目标和影响；历史会话授权、笼统长期授权或对相邻动作的授权不自动沿用。
- 优先解决根因和边界问题，不用层层 fallback、默认成功或隐藏错误制造表面稳定。
- 控制修改范围，但“范围小”不能成为交付不完整、证据不足或留下明显不一致的理由。
- 不混入与目标无关的重构、格式化、依赖升级或目录搬迁；发现相邻问题时先记录，只有它阻断当前目标或用户明确扩展范围时才处理。
- 完成可分割步骤后执行对应的最小验证；最终说明已验证和未验证内容。
- 修改长期规则时同步检查文档、自动化和远程配置模板是否仍然一致。

## 工作区与变更范围

- 工作区可能同时存在已暂存、未暂存、未跟踪或其他协作者写入的内容；这些内容默认属于用户，不覆盖、不回滚、不顺手提交。
- 修改与用户变更重叠时先理解现有差异并进行最小兼容编辑；无法安全合并时停止写入并说明冲突，不以重写整文件规避判断。
- 破坏性操作前必须解析并复核精确目标；不得对仓库根、用户目录、未解析变量或宽泛 glob 执行递归删除、覆盖或批量移动。
- 临时脚本、日志、截图、Evidence 和调试产物放在任务专用且已忽略的位置；只清理本任务创建且确认无保留价值的内容。
- 不为通过格式门禁批量改写第三方、上游镜像、生成文件或明确需要保留原格式的资料。
- 修改完成后复核 `git diff`、`git status` 和未跟踪文件，确认没有越界文件、秘密、缓存、意外换行或权限变化。

## 文档真相源

`docs/` 是正式项目文档源，各层职责如下：

- `docs/status/current.md`：当前阶段、近期决策和当前验证入口。
- `docs/product-definition.md`：长期定位、命名、核心原则和产品边界。
- `docs/adr/`：已经接受或废弃的架构与治理决策。
- `docs/governance/`：仓库操作、PR、CI 和 GitHub 远程策略。
- `docs/licensing-strategy.md`：开放基础层、商业化与贡献授权策略。
- 后续专题目录：语义、IR、Evidence、验证器、运行时和评测的正式设计。

规则：

- 代码与文档冲突时，先判断哪一方过期，再统一修正。
- 优先更新已有正式文档，不为一次性讨论创建大量平行文档。
- 入口文档保持简洁；历史推演、验证流水和实验记录放入专题或归档。
- 当前阶段、临时优先级和易过期验证命令不复制到多个长期协作文件。
- 新增或大改长文档时，在开头说明用途、目标读者和明确不包含的内容；不要让同一文件同时承担入口、规范、历史流水和完整参考四种职责。
- 文档篇幅按阅读成本而非机械行数治理。参考目标：`docs/status/current.md` 8 KB、入口 / 索引 10 KB、ADR 12 KB、Guide / Runbook 15 KB、Architecture / Boundary 25–30 KB；接近预算时优先拆成入口摘要与专题正文。
- `docs/records/`、实验记录、周志和归档只保存事实与证据，不自动成为当前规范；正式结论必须回写对应产品、专题、ADR 或状态文档。
- 引用兄弟项目时优先使用项目名和稳定在线地址；任务临时提供的本机路径不得进入长期文档。

## Agent 协同文件

- `AGENTS.md` 与 `CLAUDE.md` 必须逐字一致，不为特定工具维护分叉规则。
- 修改通用协作规则、执行边界、验证入口或阶段约束时，两份文件必须在同一变更中更新。
- 当前阶段、临时重点、易过期命令和长背景留在 `docs/status/current.md` 或专题文档，不复制到协作入口。
- 仓库检查器负责验证镜像一致性；不能通过放宽检查来容忍内容漂移。

## 项目快速认知

- 定位：面向 AI Agent 的验证优先语言与可信语义层。
- 源文件扩展名：`.rax`。
- 编译器：`raxc`。
- 核心中间表示：Axiom IR。
- 验证报告：Axiom Evidence。
- 许可证：Apache License 2.0。
- 当前阶段：定义与仓库治理初始化，技术栈和验证后端尚未冻结。
- 稳定主线：`master`；日常集成：`dev`。
- 仓库级验证：`./scripts/check-repo.sh` 或 `pwsh ./scripts/check-repo.ps1`。

## 语义与可信性约束

以下原则优先于表面语法便利和演示效果：

1. 正确性只能相对于明确规范成立，不把“满足规范”写成“符合真实意图”。
2. 外部调用、求解器假设、未验证代码、运行时依赖和可信输入必须显式进入信任边界。
3. 验证状态至少区分 `proved`、`checked`、`unknown`、`failed` 和 `trusted`，不得压缩成成功 / 失败二值。
4. 文件、网络、时间、随机性、共享状态和外部能力必须通过显式效果或能力边界表达。
5. 同一语义应尽量只有一种规范化表示；语法只是语义模型的投影。
6. 验证失败应优先产出最小失败约束、反例和可追踪路径。
7. Axiom Evidence 必须稳定、版本化、可导出，并能够由小于生产验证器的独立检查器复核。
8. 任何“proved”声明都必须能追溯验证义务、采用的规则、假设、工具版本和未覆盖边界。
9. 动态测试、静态检查、模型推断和形式证明是不同证据级别，文档与 UI 不得混用术语。
10. 规范变化本身是高风险变化，必须同时审查规范是否过弱、遗漏真实约束或引入错误假设。

## 设计与实现边界

- 先定义类型化语义、Axiom IR、验证义务和 Evidence，再冻结表面语法。
- 首个版本选择边界清楚的窄领域，不直接建设通用语言、完整 IDE、包管理、宏系统或任意并发。
- 参考现有语言、论文和兄弟项目时提炼原则与接口，不复制不兼容许可证代码、数据或实现结构。
- 不为“未来可能需要”提前建立空泛的 manager、helper、adapter、context 或多层框架。
- 抽象必须减少真实重复、隔离稳定边界、降低复杂度或表达明确领域概念。
- 参考验证器、优化验证器和独立检查器的职责必须可区分，不能用同一实现自证正确。
- 生成产物和用户程序不应因使用编译器而被意外施加项目许可证；嵌入运行时或模板时单独确认授权边界。

## AI 与自动化执行边界

执行会改变外部状态、依赖、进程或用户数据的动作前，应说明精确命令或操作、目标、主要副作用、预计持续时间和清理方式。实际动作若实质扩大范围，必须重新获得授权。

可在任务范围内直接执行：

- 读取、搜索、编辑仓库文件；
- 文本卫生、格式化、静态分析、构建和测试；
- `git status`、`git diff`、`git log` 等只读 Git 操作；
- 用户明确要求后的暂存和提交。

需要用户明确授权或单独确认：

- 安装、升级或移除依赖以及会改变 lockfile 的命令；
- 启动长期运行的服务、桌面应用或需要人工交互的进程；
- 写入 GitHub Rulesets、仓库设置、Secrets、环境或其他远程状态；
- push、创建 PR、发布包、创建 tag / Release、部署或发送外部消息；
- 修改系统配置、权限、证书、密钥链、注册表或全局工具；
- 破坏性 Git 操作、共享分支历史重写或删除数据。

默认不做：

- 跨工作区修改兄弟项目；
- 把兄弟项目文件整套复制进本仓库；
- 把一次性本机路径、凭据或环境状态写入长期文档；
- 把沙盒、网络或工具异常直接归因于代码；
- 用管理员 bypass 取代正常 PR 流程。

补充规则：

- AI 启动的后台服务、桌面程序、浏览器或临时代理，任务结束前应按约定关闭并报告；用户自行启动的进程不擅自停止。
- 关键验证因沙盒、权限、PATH、网络、证书或平台差异失败时，先保留原始错误并判断环境特征；必要时申请在更真实环境中复验同一命令，不为适应异常环境修改产品代码。
- 命令成功退出不自动代表目标达成；仍需检查预期产物、状态变化、诊断和负向结果。
- 凭据、token、私钥、恢复码和真实敏感输入不得出现在命令参数、日志、截图、fixture、Evidence、提交或对话摘录中。

## 依赖、第三方材料与生成产物

- 标准库或现有依赖能够清晰解决问题时，不为便利新增依赖；需要新增时说明用途、版本、维护状态、许可证、供应链风险和可替代方案。
- 依赖声明和 lockfile 通过对应包管理器维护，不手工伪造解析结果；意外 lockfile 漂移必须查明原因。
- 引入第三方代码、数据、模型、字体、示例或资产时记录来源、版本、许可证和必要归属，不把“公开可访问”误认为“可自由再分发”。
- 生成代码、绑定、schema、fixture 或 Evidence 必须能追溯生成入口和输入；需要提交时同时明确源文件、再生成方式和人工修改边界。
- 测试、文档和示例使用合成或脱敏数据，不复制真实用户内容、专有输入、安全报告或生产凭据。

## 文件与代码规范

- 仓库自有文本使用 UTF-8 无 BOM、LF、末尾换行且无尾随空格。
- 换行、缩进和二进制类型遵循 `.editorconfig` 与 `.gitattributes`。
- 本地状态、缓存、生成物和凭据遵循 `.gitignore`，不得为了方便提交环境文件。
- 命名必须表达语义或领域职责，不使用含义不清的缩写和技术包装名。
- 错误必须保留上下文和真实原因，不静默吞掉 `unknown`、验证失败或外部能力错误。
- 控制流优先早返回、显式分支和小而清晰的函数；不写影响阅读的函数套函数、回调深层嵌套或只转发参数的 wrapper。
- 注释解释原因、约束和不明显的权衡，不复述代码；行为变化时同步更新或删除失效注释。
- 新增公共格式必须包含版本或明确的演进策略，并提供正例、负例和兼容性测试。
- 单个文件职责应清晰；源码接近 1000 行时评估按领域或稳定边界拆分，原则上不继续超过 1500 行，生成文件和有来源的上游材料除外。
- 目录优先浅层职责分组；稳定入口留在目录根，内部实现、fixture 和 helper 进入含义清楚的子目录，不制造无意义深层树。
- committed 相对路径默认不超过 180 字符，单文件默认不超过 10 MiB；较长语义进入结构化元数据，不持续拉长路径和文件名。

## 验证策略

- 小型文档改动至少检查链接、术语、镜像文件与 `git diff --check`；治理改动还必须运行仓库级检查并复核文档、workflow、Ruleset 和检查器契约。
- 普通实现改动执行对应模块的格式化、静态分析、构建和精准测试；跨模块、公共接口或用户主路径变化再扩大到集成、端到端或运行态验证。
- 语义、IR、Evidence 或信任边界改动必须增加正例、负例、反例与兼容性验证。
- 修复健全性问题时先加入能暴露错误结论的回归用例，再修正实现。
- 涉及独立检查时，测试必须覆盖生成器和检查器结论不一致的拒绝路径。
- 涉及资源消耗或恶意输入时，验证终止、超时、内存和输入规模边界。
- 静态检查、mock 或 headless 结果不能替代其未覆盖的真实入口、平台行为、外部工具交互或人工可见结果；是否补运行态证据按风险决定。
- 若后续提供 quick / fast 模式，它只用于开发反馈，不自动替代阶段合并或发布前的正式门禁。
- PR 只记录实际执行过的验证；未执行或受环境阻塞的检查必须明确写出。
- 验证失败先保存命令、退出码和关键错误，再判断代码、数据、测试、工具或环境根因；不得只因重跑成功就忽略不稳定性。
- 技术栈冻结后，把稳定检查接入 `Candidate Quality` 聚合 job，Ruleset 仍只绑定这一稳定 context。

## Git 与 Pull Request

- 提交使用 Conventional Commits：`feat`、`fix`、`docs`、`refactor`、`test`、`chore`、`ci`、`build`、`perf`、`revert`。
- 提交按可审阅主题拆分，不混入无关重构，不添加 AI 协作者署名。
- 暂存前复核完整 diff，默认按精确路径加入本批文件；工作区存在无关变更时不使用会扩大范围的批量暂存。
- 小型提交使用清楚的单行主题；大型提交在正文用 3–6 条简短说明记录主要变化和重要边界。
- 未经明确要求不 amend、重写、合并或拆分既有提交，不修改用户 Git 身份，不用 reset、rebase 或 force push 处理共享历史。
- commit 授权不包含 push、PR、tag、Release、部署或远端设置写入；这些动作必须分别确认。
- 提交后复核提交摘要和工作区状态，报告 commit hash、剩余改动以及相对远端的 ahead / behind 状态。
- 普通变更进入 `dev`；阶段性稳定化从 `dev` 向 `master` 发 PR；紧急修复使用 `hotfix/*`。
- `master` 禁止直接 push、force push 和删除，只允许 merge commit 与 rebase merge，禁用 squash merge。
- `dev -> master` 优先 merge commit；任何 `master` 合并完成后都必须在下一轮开发前回流 `dev`。
- 分支、PR、Ruleset 和回流细节以 `docs/adr/0001-branch-and-pr-governance.md` 为准。

## 文档与治理更新

- 产品定位、命名或长期边界变化时更新产品定义。
- 当前阶段、下一决策或验证入口变化时更新当前状态。
- 架构、语义、公共格式或治理决策变化时新增或更新 ADR / 专题文档。
- 分支或门禁变化时同步 ADR、治理文档、Ruleset、workflow、PR 模板和协作文件。
- 许可证、贡献授权或商业边界变化时同步 LICENSE、许可策略、CONTRIBUTING 和 README。
- 使用 Asia/Shanghai（UTC+8）记录项目日期。

## 完成与交接

- 最终总结先说明实际结果，再列关键文件或行为变化，不用过程叙述掩盖交付状态。
- 明确列出实际运行的验证及结果；没有运行、失败、被跳过或只能静态复核的项目分别说明。
- 报告仍存在的风险、假设、`unknown` / `trusted` 边界、兼容性影响和需要人工完成的外部动作。
- 报告工作区是否干净、是否存在未提交或未推送提交、后台进程和任务专用临时产物。
- 只有必要工作和验证已经完成时才表述为“完成”；占位实现、待授权远端动作或关键验证阻塞必须显式保留。

## 通用开发原则

1. 交付小而完整的真实纵向切片，不以占位、演示路径或玩具式最小实现冒充目标完成。
2. 验证按风险分层：小改动精准验证，公共边界、核心流程、数据一致性和可信声明扩大验证。
3. 代码优先清晰、直观和可维护；抽象、模式或元编程只有在明显降低复杂度时才引入。
4. 修改前理解现有模块、数据与调用边界，沿用稳定结构；不做无关重构，但修正直接阻断当前目标的局部结构问题。
5. 不用异常吞噬、默认值、重试或多层 fallback 制造表面安全；所有兜底都必须有明确触发条件、可观测性和停止线。
6. 优先显式类型、结构化数据、标准能力和顺序控制流，避免动态字符串状态、全局隐式状态和不必要嵌套。
7. 在需求完整和质量成立的前提下控制修改范围；每个新增文件、依赖、抽象和兼容层都必须有当前用途。
8. 结论不能超过证据能力：测试通过、静态检查、模型推断和形式证明必须使用各自准确术语。

## 判断顺序

1. 需求和声明是否完整、准确。
2. 语义、信任和证据边界是否清晰。
3. 架构与兼容性是否可长期维护。
4. 修改范围和实现复杂度是否受控。
5. 验证是否与风险匹配，且结论没有超过证据能力。
