# RadishAxiom 协作约定

本文件为人工协作者与 AI 协作者提供启动级长期约束。它只约束 RadishAxiom，不复用 Radish 家族其他项目的实现边界，也不承载当前阶段、临时门禁、历史记录或专题实现细节。

## 定位与优先级

- `docs/` 是正式项目文档源；当前阶段、近期顺位、停止线和“当前不做”以 `docs/status/current.md` 为准，长期产品、架构、治理和格式契约以对应专题或 ADR 为准。
- 项目所有者在当前任务中的明确要求优先于默认协作流程；若要求会改变语义、信任、公共格式、兼容性、许可证、依赖或外部状态，仍须先说明影响并确认范围。
- 只读取当前任务需要的最小文档链；需要追溯事实时再进入 records、实验记录或历史材料，不默认展开无关兄弟项目。
- 文档、实现和自动化不一致时，先判断哪一方过期，再在任务范围内统一修正；不通过放宽检查或增加 fallback 掩盖漂移。

## 称呼与语言

- 对话开始或结束总结时称呼用户为 `萝卜SAMA`。
- 默认使用中文讨论、说明和编写项目文档。
- 代码、命令、路径、配置键、类型名、协议字段和外部项目名保留原文。
- 英文工程术语在中文可以准确表达时优先使用中文；不能准确翻译的术语首次出现时可附英文。

## 任务启动与协作原则

1. 开始任务先检查 `git status`；实际分支、ahead / behind、暂存区或工作树与用户描述不一致时先报告，不用 reset、checkout 或清理命令制造预期状态。
2. 区分本次改动、用户已有改动和并行改动；不覆盖、不回滚、不顺手提交任务范围外内容。
3. 明确请求属于说明、诊断、设计、实现、治理还是发布；一种授权不自动扩大为另一种操作。
4. 先查找现有文档、实现、测试、脚本和命名惯例；没有充分理由时不另造平行入口、重复 helper 或第二套配置口径。
5. 需求明确且风险可控时直接完成；不同解释会实质改变语义、数据、兼容性、授权或外部影响时，先说明关键判断并请求选择。
6. 优先解决根因和边界问题，交付小而完整的真实切片；不以占位、演示路径、默认成功或隐藏错误冒充完成。
7. 控制修改范围，不混入无关重构、格式化、依赖升级或目录搬迁；直接阻断目标的局部结构问题应一并收口。

更完整的工作区、实施、验证和交接规则见 [Agent 协作与执行规则](docs/governance/agent-collaboration.md)。

## 必须立即生效的执行边界

可在任务范围内直接执行：

- 读取、搜索和编辑仓库文件；
- 文本卫生、格式化、静态分析、构建和测试；
- `git status`、`git diff`、`git log` 等只读 Git 操作；
- 用户明确要求后的精确路径暂存和提交。

以下操作必须获得当前任务的明确授权，并在执行前说明精确目标、主要副作用、预计持续时间和清理或回滚方式：

- 安装、升级或移除依赖，以及会改变 lockfile 的命令；
- 启动长期运行的服务、桌面应用或需要人工交互的进程；
- 写入 GitHub Rulesets、仓库设置、Secrets、环境或其他远程状态；
- push、创建 PR、发布包、创建 tag / Release、部署或发送外部消息；
- 修改系统配置、权限、证书、密钥链、注册表或全局工具；
- 破坏性 Git 操作、共享分支历史重写或删除数据。

授权只覆盖已经说明的动作、目标和影响；历史会话、笼统长期授权或相邻动作的授权不得沿用。不得对仓库根、用户目录、未解析变量或宽泛 glob 执行递归删除、覆盖或批量移动。凭据、token、私钥、恢复码和真实敏感输入不得进入命令参数、日志、截图、fixture、Evidence、提交或对话摘录。

## 任务文档路由

| 任务 | 优先读取 |
| --- | --- |
| 当前阶段、下一步、临时门禁 | [当前状态](docs/status/current.md) |
| 产品定位、命名和长期边界 | [产品定义](docs/product-definition.md) |
| Agent 协作、工作区、验证和交接 | [Agent 协作与执行规则](docs/governance/agent-collaboration.md) |
| 分支、PR、CI 和 Ruleset | [仓库治理](docs/governance/repository-governance.md)、[ADR 0001](docs/adr/0001-branch-and-pr-governance.md) |
| 首域能力与验收边界 | [ADR 0002](docs/adr/0002-first-target-domain-and-benchmarks.md)、[首域语义](docs/semantics/keyed-finite-table-semantics.md) |
| Axiom IR 或 Axiom Evidence | [Axiom IR v0.1](docs/ir/axiom-ir-v0.md)、[Axiom Evidence v0.1](docs/evidence/axiom-evidence-v0.md) |
| 基准或 Agent 实验 | [基准语料](docs/benchmarks/keyed-finite-table-corpus-v0.md)、[实验预注册](docs/experiments/agent-representation-preregistration-v0.md) |
| 版本、实现宿主或兼容性 | [ADR 0003](docs/adr/0003-version-identities-and-compatibility-layers.md)、[ADR 0004](docs/adr/0004-raxc-production-implementation-language.md) |
| 许可证、第三方材料和商业边界 | [许可证与生态策略](docs/licensing-strategy.md) |

完整文档索引见 [docs/README.md](docs/README.md)。询问“下一步做什么”时只先读取当前状态；不足以判断时再沿索引进入专题。

## 语义与可信性红线

- 正确性只能相对于明确规范成立，不把“满足规范”写成“符合真实意图”。
- 外部调用、求解器假设、未验证代码、运行时依赖、可信输入和副作用必须显式进入信任或能力边界。
- `proved`、`checked`、`unknown`、`failed` 和 `trusted` 是不可互换的状态；动态测试、静态检查、模型推断和形式证明不得混用术语。
- 任何 `proved` 声明都必须能追溯验证义务、规则、假设、工具版本和未覆盖边界；命令成功或测试通过不能自动升级证据级别。
- 同一语义应尽量只有一种规范化表示；Axiom Evidence 必须版本化、可导出并可由小于生产验证器的独立检查路径复核。
- 参考验证器、优化验证器和独立 checker 的职责必须可区分，不能让同一生产实现自证正确。
- 规范变化是高风险变化；必须同时审查规范是否过弱、遗漏真实约束或引入错误假设，并按专题要求补正例、负例、反例和兼容性验证。

## 长期工程边界

- 先定义类型化语义、Axiom IR、验证义务和 Evidence，再冻结或扩展表面语法；首版保持边界清楚的窄领域。
- 抽象必须减少真实重复、隔离稳定边界、降低复杂度或表达领域概念；不为“未来可能需要”提前建立空泛 manager、helper、adapter 或多层框架。
- 错误必须保留上下文和真实原因，不静默吞掉 `unknown`、验证失败或外部能力错误；兜底必须有触发条件、可观测性和停止线。
- 引入依赖、第三方代码、数据、模型、字体、示例或资产时记录来源、版本、许可证与必要归属；“公开可访问”不等于“可自由再分发”。
- 生成代码、schema、fixture 或 Evidence 必须能追溯生成入口和输入；测试、文档和示例只使用合成或脱敏数据。
- 生成产物和用户程序不应因使用编译器而被意外施加项目许可证；嵌入运行时或模板时单独确认授权边界。

## Git、验证与交接

- 提交遵循 Conventional Commits，按可审阅主题拆分，使用当前用户身份且不添加 AI 协作者署名。
- 未经明确要求不 amend、重写、合并或拆分既有提交，不修改用户 Git 身份，不用 reset、rebase 或 force push 处理共享历史。
- `master` 是稳定主线，`dev` 是日常集成分支；分支、PR、Ruleset、合并和回流细节只以治理专题与 ADR 0001 为准。
- 验证按风险分层；结论不能超过实际证据。最终说明实际结果、已运行和未运行的验证、剩余风险、外部动作、工作区状态以及未提交或未推送内容。
- 修改完成后复核 `git diff`、`git status` 和未跟踪文件，确认没有越界文件、秘密、缓存、意外换行或权限变化。

## 入口文件维护

一条规则只有同时满足以下条件，才应加入 `AGENTS.md` / `CLAUDE.md`：

1. 跨任务成立；
2. 跨开发阶段成立；
3. 必须在读取专题文档前立即生效，否则容易产生高风险或大范围错误；
4. 无法只通过任务路由和对应专题可靠承载。

阶段状态、临时门禁、“当前不做”、易过期命令、批次事实和专题细节必须更新其正式真相源，不复制回根入口。两份文件必须逐字一致；修改任一文件时同步另一份，并运行仓库级检查。详细判定与内容归位规则见 [Agent 协作与执行规则](docs/governance/agent-collaboration.md)。
